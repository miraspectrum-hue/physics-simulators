# Claude Code の開発者ごとのローカル設定。
# 共有すべき設定は .claude/settings.json（こちらは追跡する）。
.claude/settings.local.json

# npm workspaces（ルートで `npm install` すると依存がここへホイストされる）
node_modules/
apps/*/dist/
*.local
.DS_Store

# Codex agent runtime state. Never commit approval or proposal state.
.agent-state/
