# CLAUDE.md - リポジトリ全体の方針

## このリポジトリについて

複数の物理シミュレータを集めたモノレポ（npm workspaces）。`apps/` 配下に、独立した
Web アプリとしてシミュレータを1つずつ追加していく。将来的にはホーム画面（カード一覧）
から各シミュレータへ SPA 風に遷移する統合ページ（`apps/shell`）を追加する計画があるが、
現時点では未着手。

## 各アプリの独立性

`apps/<name>/` はそれぞれ完全に自己完結した Vite + TypeScript プロジェクトである。
他のアプリへのコード依存を持たない（import しない）。**あるアプリで作業するときは、
そのアプリ自身の `CLAUDE.md`（開発規約）・`SPEC.md`（仕様）・`TASKS.md`（作業記録）を
必ず参照し、そちらの指示をこのファイルより優先すること。** このファイルはリポジトリ
全体に共通する最小限の方針だけを扱う。

現在のアプリ一覧:

- `apps/prism-sim/` — プリズム分光シミュレータ（詳細は `apps/prism-sim/CLAUDE.md`）

## 新しいシミュレータを追加する手順

1. `apps/<new-name>/` に、`apps/prism-sim/` と同じ形の自己完結した Vite + TypeScript
   プロジェクトを作る（独自の `package.json`・`tsconfig.json`・`vite.config.ts`・
   `CLAUDE.md`・`SPEC.md`・`TASKS.md` を持たせる。`apps/prism-sim/` を雛形として
   参照してよい）。
2. `package.json` の `"name"` はディレクトリ名と一致させる（例:
   `apps/prism-sim/package.json` の `"name": "prism-sim"`）。npm workspaces が
   このフィールドでパッケージを解決するため、ずれるとルートからの操作が壊れる。
3. 他の `apps/*` のコードは import しない（完全独立の方針）。複数シミュレータ間で
   コードを共有したくなった場合の扱いは「将来の統合方針」を参照——今は共有層を
   作らないと決めている。
4. ルートの `README.md`「シミュレータ一覧」に1行追記する。

## コマンドの使い分け

```
npm install                     # ルートで実行し、全 apps の依存を一括解決する
npm run test                    # ルートで実行し、全 apps のテストを横断実行する
npm run build                   # ルートで実行し、全 apps を横断ビルドする
cd apps/<name> ; npm run dev    # 個別アプリの開発サーバを起動（ルートには置かない）
```

**`dev` はルートに置かない。** `test`/`build` は「全 apps に対して一括実行し、結果を
待って終了する」操作なので `--workspaces --if-present` によるフォールスルーが自然だが、
`dev` は1つのアプリを起動し続けるインタラクティブなコマンドである。apps が複数になると
「ルートで `npm run dev` を叩いたときどのシミュレータを起動すべきか」が曖昧になるため、
個別アプリのディレクトリへ移動してから起動する運用にしている。

個別アプリだけを対象にしたい場合は `npm run <script> --workspace=apps/<name>` も使える。
各アプリ固有のコマンド・テスト方針は、そのアプリの `CLAUDE.md` に従うこと。

## ドキュメントの置き場所

プロジェクト固有の `CLAUDE.md`/`SPEC.md`/`TASKS.md`、および参考資料（デザイン参照画像や
使い捨ての検証スクリプトなど）は、各アプリ自身の `apps/<name>/` 配下に置く。リポジトリ
全体に関わる内容（このファイルや各アプリへのリンク集）だけをルートの `CLAUDE.md`/
`README.md` に書く。「このアプリの話かリポジトリ全体の話か」で置き場所を判断すること。

## 将来の統合方針（未着手・参考情報）

複数アプリが揃った段階で `apps/shell` を新設し、各アプリを SPA 風に組み込む予定。
その際、各アプリは自動起動する既存の `main.ts` に加えて、組み込み用の
`export function mount(container: HTMLElement): () => void` を `src/lib.ts` として
公開する規約にする（共有パッケージは作らず、この関数シグネチャの規約だけで統合する）。
着手時はこのファイルと各アプリの `CLAUDE.md` の両方を更新すること。
