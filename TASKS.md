# TASKS.md - 実装タスク一覧

## 優先度ガイド

- 🔴 **高**: 必須機能・ブロッカー
- 🟡 **中**: 重要だが後回し可能
- 🟢 **低**: あれば嬉しい・オプション

**進め方**: フェーズ順に実装し、各フェーズの完了条件を満たした時点でユーザーの確認を仰ぐ。完了したタスクは `[ ]` → `[x]` に更新する。

---

## Phase 0: 環境構築

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 0-1 | Vite + TypeScript プロジェクト作成（`prism-sim/`） | 🔴 高 | [x] | - |
| 0-2 | 依存の導入とバージョン固定（下表のとおり、キャレット無し）、`tsconfig.json` の strict 設定 | 🔴 高 | [x] | 0-1 |
| 0-2b | install → `npm run build` / `npm run test` が通ることの確認（組み合わせ未検証のため） | 🔴 高 | [x] | 0-2 |
| 0-3 | ディレクトリ雛形の作成（CLAUDE.md の構成どおり。`f9d86ea` で完了。監査で判明したチェック漏れ） | 🔴 高 | [x] | 0-1 |
| 0-4 | README（起動手順）の作成（`.gitignore` は `f9d86ea` で完了済み。残るは README のみ） | 🟢 低 | [ ] | 0-1 |

### 固定バージョン（確定・キャレット無し）

| パッケージ | 固定版 | 区分 |
|-----------|--------|------|
| `three` | `0.185.1` | dependencies |
| `@types/three` | `0.185.4` | devDependencies |
| `typescript` | `5.9.3` | devDependencies |
| `vite` | `8.2.1` | devDependencies |
| `vitest` | `4.1.10` | devDependencies |

Node.js は 20.19 以上または 22.12 以上が必要（Vite 8 の engines 要件）。

### Phase 0 完了条件
- ✅ `npm run dev` で localhost:5173 に空の黒画面が表示される
- ✅ `npm run test` がテスト 0 件で正常終了する
- ✅ `npm run build` が成功する
- ✅ `package.json` の依存が上表の完全な番号で固定されている（`^` を含まない）
- ✅ ディレクトリ構成が CLAUDE.md の記載と一致する

---

## Phase 1: 光学コア（描画なし・テスト駆動）

> **このフェーズは必ずテストを先に書く。** 描画層より先に光学計算を確定させることで、後工程で「虹が出ない」問題が起きた際に計算バグか描画バグかを切り分けられるようにする。

### 1-1: 型と定数

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 1-1-1a | `types/optics.ts` の**幾何型**（Vec3 / Ray / Plane）※型宣言のみ。three を import しない | 🔴 高 | [x] | - |
| 1-1-1b | `types/optics.ts` に Segment / LightPath を追加（tracer で必要になる時点で） | 🔴 高 | [x] | 1-4-3 |
| 1-1-1c | `PrismMaterial` を constants.ts から types/optics.ts へ移設 | 🟡 中 | [x] | 1-1-1a |
| 1-1-2 | `optics/constants.ts`（波長範囲・頂角 60°・追跡上限 6・4 材質の定数・基準線 λ_d/λ_F/λ_C） | 🔴 高 | [x] | - |

### 1-2: 分散とスペクトル

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 1-2-1 | `optics/dispersion.ts`：Cauchy 式 `n(λ)=A+B/λ²`（nm→µm 変換込み・不正入力は RangeError） | 🔴 高 | [x] | 1-1-2 |
| 1-2-1b | 分散誇張倍率 `n'(λ) = n_d + m × (n(λ) - n_d)`（m = 1〜10）。`exaggerateIndex` ＋ `traceSpectrum` の `exaggeration` 引数（既定 1・m=1 は実屈折率を厳密に返す）。m スライダー UI は 4-4 | 🔴 高 | [x] | 1-2-1 |
| 1-2-2 | **テスト**: 全 4 材質の n(587.56nm) がカタログ n_d と ±0.001 で一致 | 🔴 高 | [x] | 1-2-1 |
| 1-2-3 | **テスト**: 全 4 材質のアッベ数が実測値と ±1.0 で一致 | 🔴 高 | [x] | 1-2-1 |
| 1-2-3b | **テスト**: 単調性（n(400nm) > n(700nm)）と不正入力（RangeError）の検証 | 🔴 高 | [x] | 1-2-1 |
| 1-2-6 | `optics/fresnel.ts`：臨界角 `criticalAngle(nFrom, nTo)` と**界面単位**の全反射判定 `canTransmit(nFrom, nTo, incidenceAngleDeg)`（θ ≥ θc を全反射とする） | 🔴 高 | [x] | 1-2-1 |
| 1-2-6b | **テスト**: 臨界角の既知値・境界規約・異常系（RangeError）の検証 | 🔴 高 | [x] | 1-2-6 |
| 1-2-6c | **プリズム単位**の透過可否 `canTransmitThroughPrism(apexAngleDeg, n)`（条件 `A < 2·θc`）を `optics/prism.ts` に配置。θc は fresnel.criticalAngle に委譲 | 🔴 高 | [x] | 1-2-6, 1-4-0c |
| 1-2-7 | **テスト**: BK7/SF10/水 で true、ダイヤモンドで false を返すこと | 🔴 高 | [x] | 1-2-6c |
| 1-2-4 | `optics/spectrum.ts`：波長サンプリング（連続 48 / 7 色）と λ→sRGB 変換 | 🔴 高 | [x] | 1-1-2 |
| 1-2-5 | **テスト**: 検算表の既知値（700/546/436/380/750 と区間境界）・可視域外が黒・値域 [0,1]・境界の連続性 | 🟡 中 | [x] | 1-2-4 |

### 1-3: 幾何とレイ交差

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 1-3-0 | `optics/vec3.ts`：Vec3 代数 11 関数（純粋・不変・Three 非依存） | 🔴 高 | [x] | 1-1-1a |
| 1-3-0b | **テスト**: 全 11 関数（生成の異常系・既知値・交換法則/反交換律・正規化の零ベクトル） | 🔴 高 | [x] | 1-3-0 |
| 1-3-0c | `optics/convexSolid.ts`：**レイ⇔平面**交差（Ray/Plane 生成・pointOnRay・signedDistanceToPlane・intersectRayPlane） | 🔴 高 | [x] | 1-3-0 |
| 1-3-0d | **テスト**: 既知の軸並行幾何（正面 / 後方 t<0 / 面上 t=0 / 平行 null / 斜め 2√2） | 🔴 高 | [x] | 1-3-0c |
| 1-3-1 | `optics/convexSolid.ts`：正三角柱の平面集合生成 `createTriangularPrism`（**外向き**法線 5 枚・頂角 60° 固定・寸法は引数） | 🔴 高 | [x] | 1-3-0c |
| 1-3-1b | `optics/convexSolid.ts`：同じ寸法から頂点を返す `createTriangularPrismVertices`（Vec3 6 個）。描画メッシュと平面集合を同一の引数から生やし、絵と光路がズレる経路を断つ | 🔴 高 | [x] | 1-3-1 |
| 1-3-1c | **テスト**: 全頂点が全平面の内側または面上・各頂点がちょうど 3 面上・既知の頂点座標・頂点から導いた面中心が tracer の入射点と一致・別寸法での整合・異常系 | 🔴 高 | [x] | 1-3-1b |
| ~~1-3-2~~ | ~~変換行列を適用した平面集合の導出（任意姿勢に対応）~~ **不要化**。局所空間で追跡する方針に変更したため、平面の変換は行わない。ワールド⇔局所変換は Phase 2 の 2-3 へ移設 | — | — | — |
| 1-3-3 | スラブ法によるレイ交差 `intersectRayConvexSolid`（`t_enter` / `t_exit` と入口・出口の面） | 🔴 高 | [x] | 1-3-0c |
| 1-3-4 | **テスト**: 貫通・内部始点・後方・非交差・かすめ・斜め・非有界の各ケース（手組みの立方体で検証） | 🔴 高 | [x] | 1-3-3 |
| ~~1-3-5~~ | ~~**テスト**: 回転・平行移動後も交差結果が幾何的に整合すること~~ **不要化**。optics は姿勢を知らないため、この検証は Phase 2 の 2-3b（scene 層の変換テスト）へ移設 | — | — | — |

### 1-4: 光路追跡

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 1-4-0 | `optics/refraction.ts`：**スカラー**のスネル則 `refractionAngleDeg(nFrom, nTo, θ₁)`（全反射判定は fresnel.canTransmit に委譲） | 🔴 高 | [x] | 1-2-6 |
| 1-4-0b | **テスト**: 曲がる向き・既知値と可逆性・臨界角近傍・異常系・fresnel との境界一致 | 🔴 高 | [x] | 1-4-0 |
| 1-4-0c | `optics/prism.ts`：偏角 `prismDeviationDeg` と最小偏角 `minimumDeviationDeg`（スカラー。全反射は refraction の RangeError を伝播） | 🔴 高 | [x] | 1-4-0 |
| 1-4-0d | **テスト**: 対称通過と共役性・δ_min 既知値（SPEC 参考値と ±0.05°）・ダイヤモンドの全反射・異常系・出射面の境界一致 | 🔴 高 | [x] | 1-4-0c |
| 1-4-1 | `optics/tracer.ts`：**ベクトル形式**スネル則（屈折）※1-4-0 とは別物（3D 方向ベクトルを扱う） | 🔴 高 | [x] | 1-2-1, 1-3-3, 1-4-0 |
| 1-4-2 | 全反射の判定（fresnel.canTransmit に委譲。判別式 k<0 は使わない）と反射ベクトルの算出 | 🔴 高 | [x] | 1-4-1 |
| 1-4-3 | 多重反射ループ（上限 6 回）と射出後の延長処理 | 🔴 高 | [x] | 1-4-2 |
| 1-4-4 | 波長サンプル分の一括実行 → `LightPath[]` の生成（`traceSpectrum`。波長リストは引数で受け取るため 1-2-4 に依存しない） | 🔴 高 | [x] | 1-4-3 |
| 1-4-5 | **テスト**: 垂直入射で第一面の偏向が 0 になること | 🔴 高 | [x] | 1-4-1 |
| 1-4-6 | **テスト**: 最小偏角が `n = sin((A+δ_min)/2)/sin(A/2)` を満たすこと | 🔴 高 | [x] | 1-4-4 |
| 1-4-7 | **テスト**: 臨界角を超えた入射で全反射が起きること（フラグは持たず、内部区間が 2 本以上になることで表す） | 🔴 高 | [x] | 1-4-2 |
| 1-4-8 | **テスト**: 常に δ(赤) < δ(紫) が成立すること | 🔴 高 | [x] | 1-4-4 |
| 1-4-9 | **テスト**: 出力に NaN / Infinity が一切含まれないこと（境界条件を含む） | 🔴 高 | [x] | 1-4-4 |
| 1-4-10 | **テスト**: BK7/SF10/水 の δ_min が SPEC.md の参考値と ±0.05° で一致 | 🔴 高 | [x] | 1-4-6 |
| 1-4-11 | **テスト**: ダイヤモンドは入射角 -89〜89° の全域で入射面→出射面の直接透過が生じないこと（全反射後に底面から抜ける経路は存在する。SPEC.md「ダイヤの全反射」参照） | 🔴 高 | [x] | 1-4-4 |

### Phase 1 完了条件
- ✅ `npm run test` が全て通る
- ✅ **プリズムの局所空間**で、任意の入射レイに対し `LightPath[]` を返せる（姿勢の反映は Phase 2 の責務）
- ✅ かすめ入射・全反射・ビーム外しのいずれでも例外を投げず、NaN を返さない
- ✅ `src/optics/` が Three.js のシーングラフと DOM に一切依存していない

---

## Phase 2: 3D 描画

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 2-1 | `SceneManager.ts`：レンダラ・カメラ・ライト・レンダーループ | 🔴 高 | [x] | 0-2 |
| 2-2 | `PrismObject.ts`：正三角柱メッシュとガラス材質。**明示的な `BufferGeometry`（6 頂点 / 8 三角形）で組む**。面法線を正しく出すため頂点は非共有（24 頂点）とし、`ExtrudeGeometry` は使わない（既定のベベル・原点非対称な押し出しにより生成頂点が保証できず、平面集合との整合をテストできないため） | 🔴 高 | [x] | 2-1, 1-3-1b |
| 2-3 | `PrismObject`：**入射レイのワールド→局所変換**（`matrixWorld` の逆行列を適用）と、得られた光路の局所→ワールド変換。剛体変換のみ扱う（旧 1-3-2 から移設） | 🔴 高 | [x] | 2-2 |
| 2-3b | **テスト**: プリズムを回転・平行移動しても、局所空間での交差結果をワールドへ戻した光路が幾何的に整合すること（旧 1-3-5 から移設） | 🔴 高 | [x] | 2-3 |
| 2-4 | `BeamRenderer.ts`：`LightPath[]` → Line2 群（加算ブレンド） | 🔴 高 | [x] | 2-1, 1-4-4 |
| 2-5 | 光路更新パイプライン（dirty フラグで再計算を抑制、ジオメトリは再生成しない） | 🔴 高 | [x] | 2-3, 2-4 |
| 2-6 | 床面と背景グラデーション（`prizm.png` の雰囲気に寄せる） | 🟡 中 | [x] | 2-1 |
| 2-7 | UnrealBloomPass によるグロー表現 | 🟡 中 | [x] | 2-4 |
| 2-8 | `MaterialName` 文字列ユニオン + `MATERIALS: Record<MaterialName, PrismMaterial>` を導入し、`ALL_MATERIALS = Object.values(MATERIALS)` を導出。材質の網羅をコンパイラ強制にする（詳細は下の注記） | 🟢 低 | [x] | 4-2 |

**2-8 の背景（1-1-1c で判明・保留した課題）**

- **トリガー**: 材質選択 UI を作るとき（型付きの材質キーが必要になる時点）。それまで着手しない
- **現状の穴**: `ALL_MATERIALS` は手書きの列挙なので、材質定数を足して追記し忘れてもコンパイラは黙る
- **保留の理由**: 失敗が良性だから。誤った値でテストが通るのではなく、その材質が「静かに未検証・UI 非表示」になるだけで、消費者（UI）が現れた時点で気づく。消費者不在のうちに型を増やすのは YAGNI

### Phase 2 完了条件
- ✅ 固定の入射角・固定姿勢で、白色ビームが虹色に分散して射出する様子が描画される
- ✅ 赤が最小偏角側（頂角に近い側）に描画されている。プリズムは光を底面側へ偏向させるため、偏角が最小の赤は最も頂角寄りに残る
- ✅ プリズム内部でも既に色が分離して見える（`prizm.png` と同様）
- ✅ 光路の更新でジオメトリの再生成が発生していない

---

## Phase 3: インタラクション

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 3-1 | OrbitControls によるカメラ操作（軌道回転・パン・ズーム） | 🔴 高 | [x] | 2-1 |
| 3-2 | TransformControls（移動／回転ギズモ）と Orbit の排他切替 | 🔴 高 | [x] | 3-1, 2-2 |
| 3-3 | 姿勢変更 → 光路の即時再計算 | 🔴 高 | [x] | 3-2, 2-5 |
| 3-4 | `ui/store.ts`：アプリ状態の保持と変更通知 | 🔴 高 | [x] | 0-3 |
| 3-5 | 入射角スライダー（-89〜89°、主断面内）と光源方向への反映 | 🔴 高 | [x] | 3-4, 2-5 |
| 3-6 | プリズム姿勢スライダー（X/Y/Z 回転・位置）とギズモの双方向同期 | 🔴 高 | [x] | 3-2, 3-4 |
| 3-7 | リセットボタン（初期姿勢・初期入射角へ復帰） | 🔴 高 | [x] | 3-4 |
| 3-8 | ビームがプリズムから外れた場合に光が直進することの確認 | 🔴 高 | [x] | 3-3 |
| 3-9 | 全反射発生時も破綻なく描画されることの確認 | 🔴 高 | [x] | 3-3, 3-5 |

### Phase 3 完了条件
- ✅ プリズムを回転・移動させると光路がリアルタイムに追従する
- ✅ 入射角スライダーを動かすと分離角が連続的に変化する
- ✅ ギズモ操作とスライダー表示が常に一致している（どちらから操作しても同期）
- ✅ ビームを外す・全反射させる等の操作で描画が破綻しない

---

## Phase 4: UI パネルと数値表示

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 4-1 | `ControlPanel.ts` の骨格とダークテーマ CSS | 🔴 高 | [x] | 3-4 |
| 4-2 | 材質セレクト（BK7 / SF10 / 水 / ダイヤモンド）。光路計算の材質切替まで（ior の連動は 4-2c へ分離） | 🟡 中 | [x] | 4-1, 1-2-1 |
| 4-2c | ガラス材質の `ior` を選択材質の n_d に連動（案A: `transmission` を使わず envMap ＋ フレネル面光で表現。内部ビームの可視性を優先） | 🟢 低 | [x] | 4-2, 2-6 |
| 4-2b | ダイヤモンド選択時の警告表示（「頂角 60° では射出しません（常に全反射）」） | 🟡 中 | [x] | 4-2, 1-2-6c |
| 4-3 | スペクトル表示モード切替（連続 48 / 7 色） | 🟡 中 | [x] | 4-1, 1-2-4 |
| 4-4 | 分散誇張スライダー（×1〜×10） | 🟡 中 | [x] | 4-1, 1-2-1 |
| 4-5 | ビーム幅スライダー（表示専用。設計メモは下記） | 🟢 低 | [x] | 4-1 |
| 4-6 | `InfoOverlay.ts`：θ₁・屈折角・n(λ)・偏角 δ(赤/紫)・全反射の有無 | 🟡 中 | [x] | 4-1, 1-4-4 |
| 4-7 | 表示トグル（グロー ON/OFF、数値表示 ON/OFF） | 🟢 低 | [x] | 4-1, 2-7 |
| 4-8 | キーボード操作対応・フォーカスリング | 🟡 中 | [x] | 4-1 |
| 4-9 | ヘルプ（操作説明）モーダル | 🟢 低 | [ ] | 4-1 |

### 4-3 の設計メモ

- **切り替わるのは波長サンプル列だけ。** レンダラの種別は変わらず、両モードとも
  同じ `LightPath[]` パイプラインを通る。分岐は `wavelengthsForMode(mode)` の 1 か所
- **7 色は連続 48 の部分集合ではない**（660/610/580/510/480/450/410 は等間隔サンプルの
  格子に乗らない）。だから切替は間引きではなく差し替えになる
- **貼り替え方式（`setWavelengths`）を採り、全再生成はしない。** 全再生成は 3 つの罠を踏む——
  (a) `SceneManager.onResize` の再購読の口が無い、(b) `__debug.sectionView` のハンドルが切れる、
  (c) `SectionView` が現在姿勢で再フィットし、**モードを切り替えただけで断面図の枠が動く**。
  本数に依存する持ち物だけを 1 つの塊（`BeamSlots` / `BandSlots` / `rayLines`）にまとめ、
  そこだけ差し替える。`object`・マテリアル・シーン所属・DOM コンテナ・凍結ビューポート・
  三角形・弧・δ ラベルは持ち回る。旧ジオメトリは貼り替え時に `dispose()`
- **既定は連続 48。** `spec` を持たない共有 URL は寛容デコードで既定へ落ちるので、既定を
  7 色にすると 4-3 より前に作られた全ての共有 URL が黙って 7 色で描かれる（6-6 段階4 で
  `sa` を常に出すと決めたのと同じ忠実性）。非機能要件の「連続 48 波長で 60fps」という
  最悪ケース基準とも一致する。**SPEC.md の画面構成モックが `●7色` なのは誤り**
- **モードは store の第 5 項目に置く。** 断面図トグルが store を迂回して `aria-pressed` に
  住めたのは「計算に無影響」だからで、モードは `traceSpectrum` の入力を変える＝計算に影響する。
  `commit` の等値比較にも入れる（抜けると切り替えても通知が飛ばず無反応になる）
- **貼り替えは遷移のときだけ。** モード → 波長列は関数なので、比較するのは波長列ではなく
  **モード**である（`sampleWavelengths` は毎回新しい配列を返すので、配列同一性で比べると
  連続モード中のあらゆる操作で貼り替えが走る）。`SceneManager.applyBox` と同じ冪等ガード
- 情報バーは全反射の本数だけが実 pathCount に追従する（`48 本中 6 本` / `7 本中 1 本`）。
  n・δ・分離幅は `REPORT_RED_NM` / `REPORT_VIOLET_NM` の独立 2 波長追跡なのでビット同一
- UI はネイティブの `input[type=radio]` を同じ `name` で束ねる。矢印キーでの移動も
  ロービング tabindex も読み上げもブラウザが持っているので、`role="radio"` を手で
  組んで再実装しない。群には `fieldset` + `legend` に加えて `role="radiogroup"` を添える
- 往復の不変性は指紋で見る。**測定 URL とモードを必ず併記する**こと（正準 URL
  `#v=1&a=70&mat=sf10&sa=0.324229,0.351336,-11.371&sec=1` で連続 48 = `-856341260` /
  7 色 = `-946284801`。20 往復してもドリフトしない）
- **旧ジオメトリの dispose も別途確認済み（コミット `fb3252c` 本文より転記、5-4 のクローズ
  証拠として再掲）**：「旧ジオメトリは貼り替え時に dispose。20 往復で geometries 19 のまま」。
  これはビーム指紋（上記）とは別の指標（`renderer.info.memory.geometries`）であり、
  コミットメッセージにのみ記録されていたためここへ転記する

### 4-5 の設計メモ

- **表示専用配線（非 `AppState`）の裁定と根拠。** ビーム幅（`LineMaterial.linewidth`）は
  `traceSpectrum` の入力にならず、`geometry.attributes.instanceStart`（光路の座標＝GPU に渡す
  位置バッファ）にも一切触れない、純粋な描画側の uniform（4-5 偵察でソース読解済み）。
  よって `AppState` には入れず、`glowEnabled`/`numbersVisible`（4-7）と同じ `ShareableState` の
  独立フィールドとして置いた。配線も同型——`store.subscribe(markDirty)` を経由させず、
  `panel.onBeamWidthInput` の購読者から直接 `beams.setLineWidth`/`reflectionBeams.setLineWidth`
  を呼んだ後、`scheduleUrlUpdate()` へ直行する（`markDirty` は一度も呼ばない）。DOM 側は
  `position`/`rotation`（4-8）と同型——range 入力・非 `store` の独立購読者配列・
  `set...()` は非発火の表示専用 setter
- **★弁別器の実測 3 値（「太さは動くが座標は動かない」の証拠）。** 測定 URL
  `#v=1&a=70&mat=sf10&sa=0.324229,0.351336,-11.371&sec=1` で幅を 3.5px → 8.0px に変更：
  | 指標 | 3.5px | 8.0px | 意味 |
  |---|---|---|---|
  | `geometry.attributes.instanceStart` のチェックサム | `1058842712`（length 2304） | `1058842712`（length 2304） | **完全一致＝座標不変** |
  | `__debug.refreshBeamsRunCount()` | 2 | 2 | **不変＝再計算を焼いていない** |
  | 画素 diff（RGBA 合計閾値 30） | ノイズ床（無変更 2 枚の diff）= 0 | 3,295,382 / 3,790,160px | **表示は明確に変化** |

  ノイズ床が完全にゼロ（このアプリの描画は AA/Bloom 込みで完全決定論的）なので、8.0px 時の
  巨大な diff は測定ノイズではなく実際の変化。スクリーンショットでも目視確認済み——線が太く、
  Bloom の閾値パスがより明るい面積を拾うため halo がキャンバス広範囲に及ぶのは物理的に妥当
- **★URL エンコードの新ティア。** `shareUrl.ts` の `DECIMALS` にはこれまで2種類しか無かった
  ——世界の幾何に効く量の 6 桁組（`sourceAngleDeg`/`screenDistance`/`prismRotationDeg`/
  `prismPosition`/`anchor`/`camera`）と、倍率である `exaggeration` の 0 桁。ビーム幅は
  「世界の幾何には効かない px 表示スカラー」という点で 6 桁組ではないが、`exaggeration` と
  違って `traceSpectrum` の入力でもない（＝計算精度の要求が無い）。**どちらのティアにも
  当てはまらない、初めての「連続値かつ表示専用」のケース**として `beamWidthPx: 1`（1 桁）を
  新設した。根拠：px 表示スカラーであり世界座標には効かない／0.1px 未満はどの画面密度でも
  知覚できない／UI 側のスライダー刻みが 0.5px なので 1 桁で完全にロスレス。将来また
  「連続値かつ表示専用」の項目が増えたときの一貫性のため、この判断根拠をここに残す
- **既定値・範囲**: 既定 3.5px は現行の `BeamRenderer.ts` の `LINE_WIDTH_PX` 定数と同値
  （現状の見た目を維持し、4-5 より前に作られた共有 URL の見た目も変えない——`putNumber` の
  omit-when-default により `bw` キー自体が既定では出ないので、既存 URL はそのままクリーンに
  保たれる）。範囲は min 1.0 / max 8.0 / step 0.5（細すぎて見えない下限〜個々の光線が潰れて
  識別しづらくなる上限）
- **★jsdom の罠（4-8 の `DEFAULT_PRISM_X/Y` と同型）を実測で確認し回避した。** range 入力は
  `value` 属性を省くと仕様上 min/max の中点になるはずだが、jsdom はこれを再計算しない。
  `ControlPanel` のコンストラクタで `setBeamWidth(DEFAULT_BEAM_WIDTH_PX)` を外して実測したところ、
  jsdom は仕様上の中点（4.5）にすらならず `50` を返すことを確認（テストが実際に Red になった）。
  コンストラクタでの明示初期化により回避済み。回帰確認用のテスト
  （`4-5 段階2 A: ★jsdom罠回避`）を常設した
- **非空虚化の証明（段階1 の omit-when-default テストが実挙動を検証していることの立証）。**
  `encodeUrl` の `putNumber(parts, 'bw', ...)` 呼び出しを一時的に無条件 `parts.push('bw=...')`
  へ変異させたところ、既定省略のテスト（および `bw` を含まないキー集合を検査する既存の
  6-6 回帰テスト）が Red になることを確認。復元後、残留なしで 928/928 Green

### 4-7 の設計メモ

- **住処は sec と同じ道（`ShareableState` の独立フィールド。`AppState` には入れない）。**
  グロー（ポストプロセス）も数値表示（DOM）も `traceSpectrum` の入力に触れないため、
  4-3 で spectrumMode を store に置いた基準の裏返しとして sec 側に付く
- **sec の `markDirty`→`refreshBeams`（再計算）は継承しなかった。** 偵察で「計算に
  無影響」と書きつつ実際は毎回再計算していた sec の急所を確認済み。グロー/数値表示の
  クリック購読者は `markDirty` を一切呼ばず、`bloomPass.enabled` の切替（次の RAF で
  自動反映）と CSS の `display` 切替だけで完結させた。sec 側の再計算は既存 debt として
  残置（今回のスコープ外）
- **URL 追従は `refreshBeams` を経由しない分、`scheduleUrlUpdate` を購読者から直接呼ぶ。**
  既存の `onBeamsRefreshed = scheduleUrlUpdate` は「dirty の源はすべて refreshBeams を
  通る」という前提に立っていたが、グロー/数値表示はその前提から意図的に外れる唯一の例外
- **URL キーは省略側（sec と同型）。** 既定 ON なら `glow=`/`nums=` は出ない。
  計算モードの enum（`spec`）は既定でも常に出す方針だったのと非対称だが、これは
  ハウススタイル（enum は忠実性優先で常に出す／表示 boolean は既定省略で URL を短く保つ）
  の意図した使い分けであり、揺れではない
- 検証用カウンタ `refreshBeamsRunCount`（`__debug` 経由、DEV限定）を追加し、クリックの
  たびに増えないことを CDP で実測して確認した

### 4-8 の設計メモ

- **X/Y 位置スライダーは `#prism-rotation` の鏡写し。** ネイティブ `input[type=range]`・
  `label[for]`・`onPositionXInput`/`onPositionYInput`（人の操作のみを拾う）・
  `setPositionX`/`setPositionY`（表示専用・非発火。ギズモ→スライダー→ギズモの
  輪にしない）。`POSITION_MIN`/`POSITION_MAX`（±3）は `ROTATION_MIN_DEG`/`MAX_DEG`
  と同格の対で、既定カメラの可視半高（≈2.69）とプリズムの外接半径（≈1.15）から
  余裕を持って収まる値。位置そのものに値域は無く（共有URLの px/py は無制限のまま）、
  range 入力である以上必要な有限境界というだけである
- **★4-7（表示専用）とは性質が逆。** 位置は世界座標の並進成分＝幾何入力なので、
  `applyPositionX`/`applyPositionY` は `markDirty()` を呼ぶ（4-7のガードは適用しない）。
  CDP実測: 位置スライダー操作で指紋が `-856341260` → `-1481176704` に変わり、
  `refreshBeamsRunCount` が `2` → `3` に増加（再計算された、が正しい）。一方、
  断面図の凍結ビューポート（`sectionTransform()`）は操作前後でビット同一
  （回転で確認済みの不変量が位置移動でも成立）
- **jsdom は range 入力の既定値を再計算しない。** 仕様上 `value` 未設定の range は
  min/max の中点になるはずだが、jsdom は "50"（min=0/max=100 前提の固定値）を返す
  だけで、`this.positionXSlider.min/max` を反映しない（段階2のテストで実際に検出）。
  ブラウザ差に依存させず、コンストラクタで `setPositionX(DEFAULT_PRISM_X)` を
  明示的に呼んで確実にした（`prismPose.ts` の定数をそのまま使う）
- **★ギズモ↔スライダーの双方向を実マウスドラッグで確認（CDP、読むな叩け）。**
  移動モード（`G`）へ切り替え、XY平面ハンドルをスクリーンショットで視認して
  座標を特定し、`Input.dispatchMouseEvent` で実際にドラッグした。
  `prism.position` が `(0,0)` → `(0.4227…, 0.4158…)` に動き、`#prism-position-x/y`
  スライダーが同じ値（`0.4`/`0.4`）へ即座に追従した。px/py は `Object3D.position`
  以外に二重保持していない（`currentPositionX/Y` は毎回読み直すだけ）
- **★スクリーンアンカー（sa）は「第三の穴」ではなかった。** コード上
  `interaction.attachTarget()` は `prism.object` にしか呼ばれておらず、
  `screen.object` は一度もドラッグ対象になっていない（ドラッグという操作系自体が
  存在しない）。実際に到達経路は「光路に合わせる」ボタン（既存・タブ順6番目）
  だけで、CDPでクリック前後にアンカーが変わることを確認した（キーボードのみで
  完全に到達可能）
- **カメラ（軌道回転・パン・ズーム）は対象外。** SPEC.md F-08 が
  「軌道回転・パン・ズーム（**マウス操作**）」と明示しており、キーボード化は
  そもそも要件ではない。文書化のみで実装しない
- **★keyboard-completeness 監査（CDP実測、パネル16要素）。**
  - タブ順 = 視覚順（16/16 一致。飛びなし）
  - フォーカスリング 16/16（`outlineStyle !== 'none'` を全要素で確認）
  - アクセシブルネーム 17/17（`label[for]` または `textContent`。ラジオはロービング
    tabindex で2個目がタブ順から外れるため、控えるコントロール数17・タブ停止数16）
  - 新設スライダー（X/Y）は矢印キーで動く（実測 `0` → `0.05`）
- **G2（情報バーの `aria-live`）: 見送り。** `InfoOverlay.update()` は毎フレーム
  呼ばれるため、単純に `aria-live="polite"` を付けると値が動くたびに読み上げが
  溢れる。実際に変化があったときだけ読ませるには「値が落ち着いてから読む」
  デバウンス機構が要り、これは 4-8 の範囲を超える別作業として見送る
- **G4（Enter キーでのボタン発火）: 解消・クローズ（2026-08-28 実機スモークで確認）。**
  CDPの合成キーイベントでは3ボタン×2手法（`rawKeyDown`/`keyDown`）すべてで
  Enterが `click` を発火せず、`window.__clickProbe` も0のままだった。Spaceは
  同条件で確実に発火しており、かつアプリ側コードにEnterを妨げる実装は一切無い
  （`ControlPanel.ts` にkeydown処理なし、`InteractionCtl.ts` のグローバル
  `keydown` は `KeyR`/`KeyG`/`Escape` のみ）ため、CDP合成入力の限界を疑っていた。
  実ブラウザ（Chrome、`npm run build` → `npm run preview`）でネイティブ `<button>` に
  フォーカスして Enter キーを押下したところ確実に `click` が発火することを実機で確認した。
  **原因はアプリ側の不具合ではなく CDP 合成入力の限界だったことが確定した**
- **実機ハードウェアでのキーボード一周を裏取り（2026-08-28）。** CDP実測（タブ順16/16・
  フォーカスリング16/16・矢印キーでの新設スライダー操作）に加え、実ブラウザ・物理
  キーボードでも Tab キーによる16停止の一周・矢印キーでのスライダー操作・全操作への
  到達を確認した
- **G3（ギズモはマウスのみ）: 解消と記録。** ギズモそのもの（ドラッグ）は
  マウス専用のままだが、回転（`#prism-rotation`、既存）・移動（新設スライダー）
  ともにキーボードで完全代替できる。ギズモは「マウスでの便宜」であり、
  到達性そのものはスライダー側で満たされる

### Phase 4 完了条件
- ✅ SPEC.md の画面構成どおりに全操作 UI が機能する
- ✅ 材質を SF10 → 水 に変えると分散幅が明確に狭まる
- ✅ 情報バーの数値が操作に追従してリアルタイムに更新される
- ✅ マウスを使わずキーボードのみで全ての操作に到達できる（4-8で達成。ただし
  (a) カメラ操作はSPEC F-08が明示的にマウス限定としており対象外、
  (b) Enterキーでのボタン発火はCDP実測が不能でG4として実機確認が未了）

---

## Phase 5: 仕上げ・検証

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 5-1 | 60 fps 維持の確認と最適化（毎フレーム `new` の排除） | 🔴 高 | [x] | Phase 4 |
**5-1 の実測（実機・GPU 描画。以後の比較基準）**

Chrome DevTools の Performance で 65 秒間、入射角スライダーの往復・材質切替・カメラ回転を含めて記録。
Bloom / 背景 / 床 / ガラス（envMap + PMREM）をすべて有効にした状態。

| 項目 | 実測 |
|------|------|
| メインスレッド稼働率 | 7.0%（Scripting 2,644ms / System 1,234ms / Painting 396ms / Rendering 291ms） |
| 1 フレームあたり | 約 0.86 ms（予算 16.7 ms の 5%） |
| Frames トラック | 全域で緑（フレーム落ちなし） |
| INP | 59 ms |
| JS heap | 12.2 – 15.6 MB。鋸歯状で必ず底へ戻る（リークなし） |

**結論: 最適化は行わない。** とくに JS heap の鋸歯の振幅が小さく GC がフレームを刺していないため、
CLAUDE.md「Three.js 運用」で保留していた **tracer のミュータブルな高速経路は追加しない**
（`src/optics/` は純粋関数のまま維持する）。6-5 で光線本数が増えたときはこの表と比較する。

| 5-2 | 極端条件の確認（かすめ入射・ビーム外し・多重全反射・頂点直撃。設計メモは下記） | 🔴 高 | [x] | 3-9 |
| 5-3 | WebGL2 非対応時のフォールバックメッセージ（設計メモは下記） | 🟡 中 | [x] | 2-1 |
| 5-4 | メモリリークの確認（材質切替・リサイズ・PNG書き出しの繰り返しでGPU資源が増え続けないか。設計メモは下記） | 🟡 中 | [x] | 4-2 |
| 5-5 | `npm run build` の成果物での動作確認（2026-08-28 実機スモークで確認。下記メモ参照） | 🟡 中 | [x] | 5-1 |
| 5-6 | README に操作方法と物理モデルの説明を追記 | 🟢 低 | [ ] | 5-5 |

### 5-2 の設計メモ

- **「頂点直撃のみ未確認・未記録」という従来の記録は誤りだった。** 偵察で判明した実態：
  同じコミット `5e63828`（3-9 の全域掃引を追加したコミットそのもの）に、掃引とは別に
  `tests/optics/tracer.test.ts` の「R. 頂点直撃」節（describe 2 個・`it` 5 個）が既に
  存在し、θ₁=0（左面中点への垂直入射で内部の光が右下頂点を正確に射抜く縮退）を
  確定値付きで検証していた。加えて 3-9 の Q. 節（179 段階の入射角掃引）自体にも
  θ₁=0 が範囲に含まれており（`-89〜89` の 1° 刻み）、この縮退ケースは「有限性」
  「例外を投げない」の 2 アサーションを 4 材質 × 3 誇張倍率ぶん、暗黙に通過済みだった。
  **今回 `npx vitest run tests/optics/tracer.test.ts -t "R\. traceRay"` を実際に実行し、
  現在も 5 件とも pass することを確認した**（`5 passed | 146 skipped (151)`）
- **下流ガードも既に実装済み。** grep で確認：`InfoOverlay.ts`・`scene/screenProjection.ts`
  （2 箇所）・`main.ts`（`exitedDeviationDeg`・`countTotalReflectionPaths`）のいずれも
  「内部区間は θ₁=0 で長さ 0 に縮退しうるため、向きの正規化に使ってはならない」
  という規約（`screenProjection.ts` のコメントでは「I-6 で確定した規約」と明記）に
  沿って実装されている。偏角は外部区間（入射・射出）だけから測り、全反射の判定は
  区間の**本数**だけを見て向きを見ない——どちらも実装を読んで確認した事実
- **★今回追加した唯一の新規作業：`convexSolid.ts` 単体の characterization テスト
  （源流でのピン）。** `tests/optics/convexSolid.test.ts` に
  「createTriangularPrism: 頂点直撃（2 平面が同時に退出候補）」を新設し、
  `intersectRayConvexSolid` へ tracer を経由せず直接、頂点直撃レイを与えた。
  3 テストとも Red-first ではなく最初から Green（1-3-3 で実装済みの正しい挙動の
  characterization）。**非空虚性は変異で証明した**：`exitPlane = currentPlane;` を
  一時的に `exitPlane = enterPlane;`（ありがちなコピペ事故を模した変異）に書き換えて
  再実行したところ、新設した 3 テストのうち 1 件（`exitPlane` と退出点の整合性チェック）
  が確実に Red になり、さらに全体では 2 ファイル・**45 テストが Red** になった
  （`intersectRayConvexSolid` の `exitPlane` は光学計算全体が依存する基礎のため）。
  変異は確認後に元へ戻し、`git diff` で差分ゼロを確認してから全緑（915 件）に復帰した
- **受容事項（人工的な tie-break は入れない）：** 頂点直撃では底面・右側面のどちらが
  `exitPlane` に選ばれるかが 1 ULP の丸め誤差で決まり、実装として**どちらの結果も
  正しい**（幾何的に同じ点を指すため）。特定の面を優先させる決定的な tie-break ロジックを
  意図的に**追加しない**——理由は 2 つ。(a) 頂点は測度ゼロの特異点であり、
  どちらの面を「正解」とするかに物理的な根拠が無い（both correct）。(b) tie-break を
  足すこと自体が新たな分岐＝新たなテスト対象を増やし、この極小確率のケースに対して
  複雑さがペイしない。新設した characterization テストは「どちらが選ばれても
  finite かつ整合している」ことだけをピンし、選択そのものを固定しない
- **他頂点・入射点＝頂点ケースの到達可能性を実コードで確定した。** 固定入射点
  （左側面中点）から、内部レイの方向角は `refractionAngleDeg` を通じて
  `(θ₂ - 30)°` の範囲に収まる。実コード（`refractionAngleDeg`）で 4 材質 ×
  入射角 ±89° の全 8 通りを実測：

  | 材質 | θ₁=-89° の方向角 | θ₁=+89° の方向角 |
  |---|---|---|
  | BK7 | -71.2375° | +11.2375° |
  | SF10 | -65.3466° | +5.3466° |
  | 水 | -78.5772° | +18.5772° |
  | ダイヤモンド | -54.4325° | -5.5675° |

  頂角（apex）に当たるには方向角 **+60°** が要るが、実測の最大値は水の
  `+18.5772°` に留まる（臨界角 `θc=asin(1/n)` が常に 90° 未満である以上、
  どんな材質でも理論上界は「θ₁→89°・n→1」の極限でも 60° にわずかに届かない）。
  左下頂点に当たるには方向角 **-120°** が要るが、実測の最小値は水の `-78.5772°`
  に留まる。**よって頂角・左下頂点はこの固定入射点からは幾何的に到達不能**——
  掃引に追加すべき R. 節相当のケースは無い（新規テストは追加しない）
- **入射点そのものが頂点に一致するケース（第 1 面での頂点直撃）は今回のスコープ外。**
  現在の掃引・R. 節はいずれも入射点を左側面の中点に固定しており、入射点自体を
  動かす検証は構造的に対象外（記録のみ。将来 3-8 のような「プリズムを動かす」
  検証で入射点が可変になったときに再検討する）

### 5-3 の設計メモ

- **偵察で確定した事実：three.js 0.185.1 の `WebGLRenderer` は WebGL1 フォールバックを
  持たない。** `contextName = 'webgl2'` がソース上ハードコードされており（three本体の
  `WebGLRenderer.js`）、`'webgl2'` のコンテキスト生成が失敗すると即座に例外を投げる。
  「WebGL1へ実際にフォールバックして描画を継続する」は現在の依存では不可能で、
  SPEC.md の「非対応時は明示的なメッセージを表示」（検出して警告、描画は諦める）が
  唯一現実的な選択肢だった
- **二段構えで無地画面を根絶した。**
  1. **事前チェック**：`three/addons/capabilities/WebGL.js` の `WebGL.isWebGL2Available()`
     を `SceneManager` 構築前に呼ぶ。`false` なら `renderWebGLFallback(container)` して
     即 `return`——`WebGLRenderer` を一度も呼ばないので、例外そのものが発生しない
  2. **スコープ限定 `try/catch`**：`isWebGL2Available()` が `true` でも GPU
     ブロックリスト等で実際のコンテキスト生成が失敗する場合に備え、
     `new SceneManager(container)` の呼び出し**だけ**を囲む。`main()` 全体は囲わない
     （無関係な後続バグまで飲み込まないため）。catch 節は `console.error(error)` で
     原因を残してから同じ `renderWebGLFallback(container)` に合流する——UX は救うが
     原因は握り潰さない
- **`renderWebGLFallback` は DOM 構造の組み立てだけを行う純粋な副作用関数**
  （`src/ui/webglFallback.ts`）。WebGL・three には一切触れない。`id="webgl-fallback"`
  で冪等判定（既に挿入済みなら何もしない）、`role="alert"` でスクリーンリーダ通知、
  クラスは新設せず既存の `.control-panel__warning`（配色）をそのまま再利用。
  **配置だけは隅の極小要素にせず、`#webgl-fallback` の ID セレクタで画面中央・大きめの
  幅（`min(480px, 100%-48px)`）に上書きした**（`main.css`）。カスケード順序に依存しない
  よう、レイアウト上書きは ID セレクタ（`.control-panel__warning` より強い）で行った
- **段階1（9件）は段階2の実装後、Red-first のまま全て Green になった。** 1件
  （canvas を生成しないことの確認）は関数の設計上つねに真になる構造的な例外として
  段階1で明記済みのとおり
- **★CDP 実測（`getContext` を書き換えて WebGL2 非対応を再現）**：
  | シナリオ | canvas | フォールバック表示 | `__debug` | console |
  |---|---|---|---|---|
  | A: 通常（WebGL2あり） | 1 | 出ない | あり | エラー0（回帰無し） |
  | B: 事前チェックで弾く（`getContext('webgl2')` を無効化） | 0 | **出る**（`role=alert`・本文一致） | 無し | **エラー0**（Uncaught に到達しない） |
  | C: `isWebGL2Available` は true だが `WebGLRenderer` 構築失敗（2引数呼び出しだけ無効化） | 0 | **出る**（Bと同一メッセージへ合流） | 無し | `console.error` に原因が1件残る（`Error: THREE.WebGLRenderer: Error creating WebGL context.` ——**「Uncaught」ではない**＝捕捉済み） |

  シナリオCの区別方法：`isWebGL2Available()` は `canvas.getContext('webgl2')` を
  引数無し（1引数）で呼ぶのに対し、実際の `WebGLRenderer` は
  `canvas.getContext('webgl2', contextAttributes)` を2引数で呼ぶ。引数の有無で
  呼び出し元を判別し、2引数呼び出しだけを失敗させることで
  「対応report はtrueなのに実際の構築だけ失敗する」状況を確定的に再現した
  （実機の GPU ブロックリスト再現は不要だった）
  - スクリーンショットで実際に読めることを確認（中央に十分な大きさ・オレンジ系警告色・
    2段落とも折り返して表示。隅の極小要素になっていない）
  - 本番ビルド（`vite build`）でも `tsc`/`vitest`/`console` すべて確認済み

### 5-4 の設計メモ

- **読み替え根拠**：元の行「材質切替の繰り返しで dispose が効いているか」は前提がズレている。
  `PrismObject.setRefractiveIndex()`（`src/scene/PrismObject.ts`）は `material.ior` と
  `rimStrength.value` を書き換えるだけで、`new` も `dispose()` も一切呼ばない。ジオメトリ・
  マテリアル・環境テクスチャはコンストラクタで一度だけ生成され、以後不変。dispose する
  新旧オブジェクトのペアがそもそも存在しないため、「dispose が効いているか」という問い自体が
  成立しない。5-4 は「インプレース書き換え（および他の資源確保箇所）で GPU 資源が増え続けない
  こと」の確認へ読み替える
- **GPU 資源を確保/解放する箇所の全数**：材質切替（インプレース、確保無し）／
  スペクトルモード切替（`setWavelengths` でジオメトリを貼り替え、旧は dispose。上の
  4-3 設計メモに転記済み）／リサイズ（`composer.setSize`/`bloomPass.setSize` は**既存**の
  `WebGLRenderTarget` を `.setSize()` で内部的に再確保するだけで新規オブジェクトは作らない。
  `EffectComposer.reset()`（dispose+差し替えを行う別メソッド）は本アプリでは一度も呼ばれない）／
  PNG 書き出し（`captureComposite.ts` は素の 2D canvas をローカル変数として使い捨てるのみで
  three.js のリソースではなく `renderer.info.memory` の対象外）／カメラ・グロー・数値・位置・
  回転（いずれも既存プロパティの代入のみで確保を伴わない）
- **★CDP 実測**（測定 URL `#v=1&a=70&mat=sf10&sa=0.324229,0.351336,-11.371&sec=1`、
  `__debug.memoryInfo()` = `{ geometries, textures, programs }`。`programs` は
  `renderer.info.programs?.length ?? 0` を返す検証用ゲッターとして本タスクで追加）：

  | 操作 | 試行回数 | geometries 前→後 | textures 前→後 | programs 前→後 |
  |---|---|---|---|---|
  | 材質切替（実 UI の `#material` セレクトに `change` を実発火、4 材質 ×30 巡） | 120 | 19→19 | 19→19 | 21→21 |
  | リサイズ（`Emulation.setDeviceMetricsOverride` で反復。試行 25 回に対し `resizeCount` は26回発火） | 25（実適用26） | 19→19 | 19→19 | 21→21 |
  | PNG 書き出し（`capturePngComposed()` を反復） | 15 | 19→19 | 19→19 | 21→21 |

  3 操作いずれも 3 指標とも増加無し。**材質切替で `programs` が増えないことを実測で確定**
  （`onBeforeCompile` によるカスタムシェーダ注入があるため再コンパイルの懸念があったが、
  `ior` は three.js の `MeshPhysicalMaterial` ではシェーダ `#define` ではなく uniform として
  扱われ、`setRefractiveIndex` は `material.needsUpdate` を一度も立てないため再コンパイルは
  発生しない）。console エラーは全操作を通して 0
- **未計測で残す項目**：GPU の実 VRAM 使用量そのものは、WebGL からは問い合わせる標準手段が無い
  （`WEBGL_debug_renderer_info` 拡張は GPU ベンダ／レンダラの識別文字列のみを返し、メモリ量は
  含まない。実測環境：`vendor: Google Inc. (Intel)` / `renderer: ANGLE (Intel, Intel(R) Arc(TM)
  140T GPU (16GB)...)`）。`renderer.info.memory`/`renderer.info.programs` が three.js アプリで
  実用上得られる最も直接的な代替指標であり、本タスクではこれ以上の下位レベル計測手段は無い
- **`tsc --noEmit` 0 エラー、`npm run build` 成功、`npm run test` 31 Test Files / 915 Tests
  passed**（`memoryInfo` の戻り値型に `programs` を追加した以外、生産コードの分岐・確保挙動は
  一切変更していない）

### Phase 5 完了条件
- ✅ 1920×1080 / 連続 48 波長で 60 fps を維持している
- ✅ 光路の再計算が 2 ms 以内に収まっている
- ✅ 極端な操作を行っても NaN 由来の描画崩れが発生しない
- ✅ ビルド成果物が静的ファイルのみで動作する

---

## Phase 6: 拡張機能（実装対象）

| # | タスク | 優先度 | 完了 | 依存 |
|---|--------|--------|------|------|
| 6-1 | `SectionView.ts`：断面 2D ビュー（正射投影の副シーン、角度ラベル付き） | 🟡 中 | [x] | Phase 5 |
| 6-2 | 最小偏角の自動探索（δ(θ₁) を数値的に最小化し、その角へアニメーション遷移） | 🟡 中 | [x] | Phase 5 |
| 6-3 | `ScreenObject.ts`：仮想スクリーンへのスペクトル投影（距離を可変） | 🟡 中 | [x] | Phase 5 |
| 6-4 | `optics/fresnel.ts` に反射率 R の算出を追加とテスト（ファイル自体は 1-2-6 で作成済み） | 🟡 中 | [x] | Phase 5 |
| 6-5 | フレネル反射光の描画（強度に応じた明るさ、分岐は 1 回まで） | 🟡 中 | [x] | 6-4, 6-3 |
| 6-6 | 設定の URL 共有 / PNG 書き出し（`6e1c3e3`/`e3c1289`/`351433a`/`735e943`/`4171b23`。設計メモは下記に既存。監査で判明したチェック漏れ） | 🟢 低 | [x] | Phase 5 |
| 6-7 | カメラ視点の共有（`cam`。position + target を複合キー1本で URL に載せる） | 🟢 低 | [x] | 6-6 |

### 実装上の注意

- **6-2**: δ(θ₁) は下に凸な単峰関数だが、その最小点には閉形式 θ₁_min = asin(n·sin(A/2)) がある。**本番経路は解析式**とし、黄金分割探索は独立オラクルとしてテスト側に置いて突き合わせた（探索の収束許容差が表示値に混ざるのを避けるため）。基準波長は d 線（587.56nm）、屈折率はカタログ値 n_d（分散誇張の軸なので m に依らない）。求まった角度に対し `n = sin((A+δ_min)/2)/sin(A/2)` が材質定数と一致すること、および 6-5a の透過率極大と一致すること（dT/dθ₁ = 0）を検証に使った。**ダイヤモンド選択時は解が存在しないため、ボタンを無効化すること**（`canTransmitThroughPrism()` で判定）。適用は光源スライダーのみを動かし、プリズムの姿勢は変えない（案 A の較正を打ち消す量を足す）。アニメーション遷移は easeInOutCubic の純粋関数に切り出し、端点を厳密一致させることで即時適用と着地がビット同一になる
- **6-1**: 描画は WebGL ではなく **SVG のインセット**（320×320、左上、トグルで出し入れ）。**物理は一切再計算しない** —— 3D ビームへ渡すのとまったく同じ `LightPath[]` を受け取り、`worldToDispersionUV()` → `uvToSvg()` で写すだけ（`SectionView` は Snell も屈折率も臨界角も import しない）。基底は `matrixWorld` の列から取るのでプリズムに乗った視点になり、Z 回転させても図の三角形は動かない。ビューポートは構築時に凍結（プリズム断面 ×2.6）し、光線で拡縮させず viewBox に切らせる。角度ラベルの数値は情報バーと同じ `InfoOverlay.formatAngle` を通し、δ は誇張中の値なので情報バーと同じ条件（m ≠ 1）で `×m` を添える。弧は `arcPolyline` で幾何的に符号化してあり、描かれた点列から測り直した掃引角がラベルの値と ±0.012° で一致する
  - **残り（任意・後回し）**: 視感度で暗くなる端の波長（380 / 750nm 付近）の底上げ、θ₂ の弧（320px では射出点まわりが混み合うため見送り）、線幅・配色のスタイリング
- **6-6**: 状態は 4 つの住処（store / `Object3D` の姿勢 / 配線側が凍結したスクリーンアンカー /
  断面図トグルの `aria-pressed`）に分かれて住む。**所有権は動かさず、シリアライズの境界で
  1 つの `ShareableState` に集める**（store を源にすると `TransformControls` がドラッグ中に
  直書きする quaternion と競合し、3-5b のエコー問題が再発する）。復元は
  **初回の凍結 → 初回 `refreshBeams()` → その後 `applyShareableState`** の順を厳守する。
  `aimPoint` / `entryNormalAngleDeg` は既定姿勢で一度だけ凍結され、共有者も同じ角度で
  凍結してから動かした以上、受け取り側も凍結後に動かすのが唯一の忠実な再現になる。
  適用は必ず既存のユーザー操作経路（`store.update` / `applyRotationDeg` / 表示専用 setter）を通す
  - **URL は hash**（`#v=1&…`）。サーバへ送られず、静的配信でも `file://` でも動く。
    書き込みは `replaceState` で、最後の変化から 300ms の静穏時間を置いて 1 回だけ
    （スライダーのドラッグや 450ms のアニメーションで履歴を汚さない）
  - **`decodeUrl` は決して投げない**（未知キー無視／欠損は既定／解釈不能も既定／範囲外は
    `clampState` へ／未知材質は BK7）。これが Phase 4/5 の項目追加を**加算的**にする。
    `v` は加算では上げず、既存キーの意味が変わったときだけ上げる
  - 桁は「入力の粒度」ではなく**「絵が動かないこと」**で決める。アンカーは光路から計算された
    値で入力粒度を持たず、3 桁だと往復で 8,786 画素成分ずれた（実測）。位置・角度・距離は 6 桁
  - **既定状態の URL も `#v=1&sa=…` になる**（`sa` は常に載る）。`collect()` は実行時に常に
    具体的なアンカーを返すためで、これは意図した挙動である。省くと受け取り側が導出し直し、
    既定値を将来変えたときに古い URL が別の絵になる。忠実再現を短さより優先する
  - **共有ボタンはその場で `collect()` → `encode()` し、保留中のデバウンスも flush してからコピーする。**
    `location.href` をそのまま読むと、静穏時間が明ける前に押されたとき 300ms 古い URL を配ることになる
  - 検証の物差しは**画素差**を正とし、ビーム指紋は補助に留める。指紋は座標を `×1e6` して丸めるが、
    位置バッファは `Float32Array` で刻みが 2^-20 ≈ 9.54e-7 ある。往復で丸めが片側だけ繰り上がると、
    描画が完全一致していても指紋は割れる（実測: 最大差 9.5367431640625e-7、画素差 0）。
    指紋が有効なのは「同一経路で描き直しても値が動かない」ことを見る不変性の検査
  - **PNG 書き出し（PNG-1）は「同一同期タスクで `composer.render()` → `toDataURL()`」で撃つ。**
    `preserveDrawingBuffer: true` はフレームごとに描画バッファの複製を保持させるので、
    たまに押される保存のために常時そのコストを払わない。描画バッファが捨てられるのは
    ブラウザが canvas を合成した後＝現在のタスクが終わった後なので、同じタスクで
    描き直して直後に読めば必ず中身がある。**この 2 行の間に `await` を挟むと空の画像になる**
    （実測: 別タスクから読むと非黒画素 0・不透明画素 0、data URL は 36,182 文字。
    同一タスクなら 1,001,382 文字で、同じフレームの `gl.readPixels` と全成分一致）
  - PNG-1 は 3D キャンバスのみ。断面図は DOM 重畳の SVG なので**写らないのが仕様**
    （断面図 ON / OFF で書き出し画像の差は全画素 0）。合成は PNG-2 の仕事
  - **PNG-2（断面図インセットの合成）で保存ボタンを WYSIWYG に一本化し、PNG-1 を吸収した。**
    ボタンは 1 つのまま、断面図が出ていれば重なった絵、消えていれば 3D だけが出る
    - 段取りは**同期の 3D スナップショット + 非同期の SVG ラスタ化**。3D は
      クリックと同じ同期タスクで撮り切り（PNG-1 の経路をそのまま使う）、撮れた文字列は
      もう描画バッファに依存しないので、そこから先の `Image` 読み込みは非同期でよい。
      `async` 関数の本体は最初の `await` まで同期で走るので、**`captureDataUrl()` を
      先頭に置く**限りこの順序は保たれる
    - **断面図が非表示なら、撮った文字列をそのまま返す。** 2D キャンバスを経由して
      詰め直すと PNG のエンコードが変わる余地を作る。何も重ねないなら何もしない、が
      最も強い保証になる（実測: 合成の data URL が PNG-1 と**文字列として完全一致**）
    - **clone 規律。ライブの SVG には指一本触れない。** 背景 rect を実際の断面図へ
      挿し込んだら画面の図が壊れる。`cloneNode(true)` した複製にだけ
      `augmentSvgForRaster` を適用する（実測: 書き出し前後で `outerHTML` が完全一致、
      注入された rect 0 個）
    - 直列化した SVG は**外側のスタイルシートを参照できない孤立文書**になる。段階0 の
      実測で落ちるのは背景・枠線と角丸・継承していた書体の 3 つだけ（`foreignObject`・
      Web フォント・`url(...)` 参照はいずれも 0 件）。焼き込まないと画面との差 92.82%、
      焼き込むと 11.70%
    - **やり過ぎない。** 自前で `font-family` を持つ要素には触れない。ラベルの色と
      ハローは可読性パスの成果であり、ラスタ化のときだけ消えては意味がない
    - **オラクルは二枚に分ける。** 3D 領域は**厳密**（インセット外はビット同一を要求）、
      インセットは**知覚**（構造が揃っていることを見る。ビット同一は求めない）。
      SVG のラスタ化は独立したフォントラスタライザを通るので、後者に厳密等号を置くと
      環境差で必ず割れる
    - 座標系が 2 つ出会うのは `computeInsetPlacement` だけに閉じる。写像に使う倍率は
      `window.devicePixelRatio` ではなく**実際のバッキング比 `canvas.width / clientWidth`**
      とする（`computeBackingSize` は `MAX_PIXEL_RATIO` で頭打ちにし floor もするので、
      DPR 3 の環境では両者が食い違う）。端数は丸めず、はみ出しは `RangeError` で止める
      （`drawImage` は枠外を黙って切り捨てるので、絵が一部だけ欠ける壊れ方をする）
    - DPR > 1 は純粋テストで担保する。実機は DPR=1 なので、写像の検証は
      `computeInsetPlacement` の単体テスト（DPR 2 / 1.5 / はみ出し 4 通り）が正面になる
    - ファイル名は PNG-1 から不変（`prism-<材質>-<角度>deg.png`）。合成の有無で変わらない
  - **ビーム位置指紋は測定状態とセットでしか意味を持たない。値だけを持ち回るな。**
    指紋は世界座標のハッシュなので、θ₁・材質・誇張倍率・姿勢・**スクリーンアンカー**に
    依存し、色・アスペクト・DPR・カメラ・書き出しには依存しない。したがって
    「動いていない」を言えるのは**同一状態どうしを比べたとき**だけである
    - 正準既定状態 `#v=1&a=70&mat=sf10&sa=0.324229,0.351336,-11.371&sec=1` → `-856341260`
      （アスペクト是正の前後で不動。PNG-2 の HEAD でも再現、2 回測って同値）
    - `sa` を省いた `#v=1&a=70&mat=sf10&sec=1` → `1022133094`（可読性パス・PNG-2 で使用）
    - 両者の差は `sa` の 1 項目のみ。スクリーン平面が動くと射出光の打ち切り点が動くため、
      末尾波長の終点だけが変わる（プリズム行列・光線の始点・情報バー・断面図ラベルは同一）
    - **報告に指紋を書くときは測定した URL を必ず併記する。** 可読性パス（f6c1b84）の
      「不変量ガード」で、実際に走らせた `legib2.mjs` の出力は `1022133094` だったのに、
      アスペクト段階の `-856341260` を持ち越して書いた実績がある。番兵の値は
      **走らせた出力からそのまま転記する**（手元の記憶や別段階の値で埋めない）
- **バッキング/表示アスペクト不整合の是正（Phase 2 以来の不具合。6-6 段階5 で発覚）**:
  `SceneManager` は構築時に一度だけ容器を測ってバッキング寸法とカメラのアスペクトを決めていた。
  その時点では操作パネル（320px）がまだ DOM に入っておらず、`window` のリサイズも起きないため、
  **バッキング 1522 × 表示 1202 の不整合が起動時に固定**されていた。`#app canvas { width:100% }` が
  バッキングを CSS 幅へ縮めるので、絵は横に 79% 潰れて表示されていた（実測: 円の幅/高さ = 0.78975
  = 1202/1522、正三角形の 3 辺が 305.5 / 251.1 / 271.2 CSS px、内角 51.2° / 57.3° / 71.5°）
  - **原因は計算式ではなく「測る瞬間」だった。** よって直し方は「初期化の末尾でもう一度測る」
    ではなく、**容器を `ResizeObserver` で監視して、観測された箱からしか寸法を作らない**
    （案B）。ワンショットの再呼び出しは今回のタイミングを 1 回塞ぐだけで、「いつ測るか」を残す
  - **監視対象は canvas ではなく容器（`#app`）。** canvas を監視するとバッキングを変える →
    canvas の箱が変わる → また通知、のフィードバックループになる。`#app` は `flex: 1` で
    大きさが CSS 側だけで決まり、canvas のバッキングに依存しない
  - 適用は RAF 整列（通知の中で寸法を書くと同じフレームでレイアウトを読み直し thrash になる）。
    同じ箱で呼ばれたら何もしない冪等ガードを置く（同サイズの resize 通知 20 回で適用回数 0 増を実測）
  - 純粋層 `computeBackingSize(boxW, boxH, dpr)` が「箱 → バッキング寸法とアスペクト」を独占する。
    **アスペクトは丸めた `canvas.width / canvas.height` ではなく箱の比から作る**。丸めから作ると
    DPR が小数のとき表示の箱と食い違う余地が残る。丸めは three の `setSize` と同じ floor
  - **`camera.aspect` の真実は箱比であり、バッキングは floor されうる。** 耐荷重の表明・検査は
    `camera.aspect === clientWidth / clientHeight` の一本にする。
    `camera.aspect === canvas.width / canvas.height` は **DPR=1 限定の情報**で、
    HiDPI（DPR 1.5 など）では floor のぶん食い違うのが正しい。ここを厳密等号で表明すると
    高解像度環境で誤検知する
  - 描画は**箱を観測してから**始める。構築時の下敷きのまま描くと潰れた絵が実際に画面へ出る
    （是正前の実測で 2 フレーム）。`ResizeObserver` の通知はそのフレームの rAF より後に届くので、
    描画ループの先頭で待機中の箱を反映し、未観測のうちは `composer.render()` を撃たない（実測 0 フレーム）
  - リサイズは `composer.setSize` / `bloomPass.setSize` へも必ず伝播させる。renderer と camera だけ
    直して composer を忘れると、書き出しの画素と画面の画素が食い違う
  - **是正の受け入れは「アスペクトだけが動いた」ことの証明**で行う。射影に依存しない量が
    ビット同一で残ること —— 情報バーの物理値 / 断面図 SVG の弧（DOM なのでアスペクト非依存）/
    pose 由来の実測 θ₁ / ビーム指紋（世界座標の位置バッファ）。1 ビットでも動けば物理回帰の混入
- **可読性パス（端の暗さとラベル被り）**:
  段階0 の実測で 2 つの不読性を確認した。基準色の Rec.709 輝度で 380nm = 0.0343 が
  ピーク 576.81nm = 0.9105 の **3.77%**。断面図の δ ラベルは背後の光線に負けて
  **δ紫 1.135:1 / δ赤 1.357:1**（θ₁ は背後が暗く 7.58:1）
  - **フロアは基準色（λ → 色）だけに置く。区間強度には触らない。** 端の暗さには
    原因が 2 つある。基準色が暗いのは**モニタ表示都合の知覚アーティファクト**なので
    持ち上げてよい。区間強度が低いのは**物理**（フレネル透過・全反射損失）で、
    情報バーが「入射面反射 20.6%」「48 本中 6 本が全反射」と語っている量そのものである。
    ここを持ち上げると絵が数値に嘘をつく。**絵は強調（波長 → 色）、数値は正直（強度 = 物理）**
  - **20% フロアは飽和青の sRGB 色域天井（Y = 0.0722）とぶつかり到達不能。**
    435.11nm の基準色 `[34,0,255]` は青成分が既に 1.0 で、色相を保つ限り上げられない。
    全 48 波長が色相を保ったまま満たせる最大は **0.075658（ピークの 8.31%）**
  - **採用＝案1：色相を厳密に保ち、フロアは色域正直な 8.31%。** 理由は、測光量 Y が
    飽和青の見えを構造的に過小評価するため（Helmholtz-Kohlrausch 効果：等輝度なら
    彩度が高いほど明るく見える）。「青の Y ≥ 20%」は**間違った計器の読み**であり、
    それを満たすために白を混ぜて彩度を壊すのは本末転倒で、七色の純度＝虹の identity を失う。
    **青は暗いまま純度を保つ**（製品決定）。白混合案（最大 11.5%）は不採用
  - `displayColorForWavelength` が波長 → 表示色の**唯一の入口**。3D ビーム・スクリーンの帯・
    断面図 SVG の光線・δ ラベルの色がすべてここを通る。二重定義を作らない
  - ラベルの可読性は**再配置ではなく縁取り**で作る（correct-by-construction）。背後の光線は
    入射角と材質で動くので事前に避けられない。縁取り色は文字色から選び、純黒・純白の
    2 択にすると下限が解析的に決まる（両者が釣り合う L = 0.179129… で **4.583:1**）。
    純黒・純白から内側へ寄せるとこの下限が 4.5 を割るので、両端は動かさない
- **6-5**: フレネル反射は分岐により光線本数を倍増させる。**追跡本数の上限管理を実装前に決めること**（既定: 反射光は 1 回のみ分岐、以降は追跡しない）。60 fps 要件を割らないよう、6-3 で本数増加時の負荷を確認してから着手する

### 6-7 の設計メモ（カメラ視点の共有）

- **住処は glow/nums と同型（4-7 の再来）。** `cameraPosition`/`cameraTarget` は
  `ShareableState` の独立フィールドで、`AppState` には入れない。視点を変えても
  世界座標も光路も動かないため（`traceSpectrum` の入力に無関係）。真実は
  `camera.position` と `OrbitControls.target` そのもの——`Object3D.matrix` が姿勢の
  単一の真実なのと同型で、別変数に二重保持しない
- **★②ガード（glow/numsと同じ・位置スライダー=4-8とは逆）を CDP 実測で確認した。**
  実マウスドラッグ（左=回転・右=パン、慣性が残っている最中に測定）で
  ビーム指紋 `-856341260` は不動、`refreshBeamsRunCount` も `2` のまま不動
  （`applyCameraView`/`interaction.onCameraChange` はどちらも `markDirty()` を呼ばない）
- **Perspective カメラでは position + target の 2 点で視点が過不足なく定まる。**
  `OrbitControls` のドリー（ズーム）は Perspective では `camera.zoom` ではなく
  `camera.position` の移動に畳み込まれる（`zoom` が動くのは Orthographic のときだけ、
  three.js の実装仕様）。`camera.up` もアプリ内で一度も書き換えられないため不変。
  この 2 点だけを URL に載せれば十分という偵察の結論を実装でもそのまま採用した
- **エンコードは `sa` 流の手動カンマ結合だが、省略判定は `sa` と違う「複合
  omit-when-default」。** `cam = px,py,pz,tx,ty,tz` を 1 キーに結合し、**6 成分すべてが
  丸め後に既定と一致するときだけ `cam` ごと省く**。1 成分でも既定と違えば 6 値まとめて
  載せる（間引かない）。`sa` は「アンカーが null か否か」という 1 フィールドの判定だが、
  `cam` は 6 数値の複合判定という点が違う（`putCamera`/`readCamera`、`shareUrl.ts`）
- **InteractionCtl の新規公開 API は 3 面だけ。`orbit` オブジェクト自体は外に漏らさない**
  （カプセル化を保つ）。
  - `cameraTarget(): Vector3` —— `orbit.target` の**複製**を返す（生の参照ではない。
    直接書き換えると内部の球面座標キャッシュと食い違うため）
  - `setCameraView(position, target): void` —— `camera.position.set()` +
    `orbit.target.set()` の直後に明示 `orbit.update()` を呼ぶ。呼ばないと次のレンダー
    ループの `update(deltaSeconds)` まで反映が不確実になる（同一フレーム確実性のため）
  - `onCameraChange(cb): void` —— `orbit` の `'change'` を中継するだけ。デバウンスは
    呼び出し側（`main.ts`）の責務
- **★デバウンスの実測で判明した非自明な事実：`enableDamping` により、ドラッグの
  『量子化1回』では済まない。** `OrbitControls` はマウスを離した後も慣性で `'change'`
  を連射し続け、実測でリリース後 **2.6〜3.1 秒**にわたり発火した（`cameraChangeRunCount`
  が 0→134 まで増加）。既存の 300ms 静穏デバウンス（`scheduleUrlUpdate`）はその間
  ずっとリセットされ続けるため、「1 ドラッグ操作 → 1 回の書き込み」ではなく
  「1 ジェスチャー（ドラッグ＋慣性の尾）→ 少数回の書き込み」が実際の挙動になる
  （実測: `urlWriteCount` が慣性の収束を待って `1`→`3` に増加。姿勢の `replaceState` と
  同じ仕組みに素直に乗せた結果で、新しい仕組みは足していない。単に慣性込みの
  収束時間が回転/位置スライダーより長いだけであり、最終的に書き込まれる URL の
  `cam` 値は正しく静止後の視点を表している）
- **★精度は画素で測った（値のビット同一ではなく、`sa` の 8,786 画素 evidence と同じ
  instrument）。** 手丸め・ドラッグ・デバウンス待ちを一切挟まない、実パイプライン
  そのものでの往復（`setCameraView` で非自明な視点を設定 → `writeUrlNow()` で
  即座に実際の `encodeUrl` を撃つ → 生成された実 URL へフルリロード →
  `capturePngComposed()` で比較）で測定した。
  | 項目 | 実測値 |
  |---|---|
  | 送信 → 受信の成分別往復誤差（position/target 6 成分） | 3.97e-7 〜 4.65e-7（6 桁丸めの理論上限 5e-7 と整合） |
  | ビーム指紋 | 送受信とも `-856341260`（不動） |
  | 差分成分数（1 でも違えばカウント） | 47,882 ／ 15,160,640（0.32%） |
  | **知覚閾値（≥8/255 = 3%超の輝度差）を超える成分数** | **13 ／ 15,160,640（0.00009%）** |
  | 最大成分差 | 58/255 |

  **「実用上絵が動かない」を測定値に置き換える：** 生の差分成分（0.32%）は 1 階調
  ズレただけの AA 縁もすべて数えるため、量としては大きく見えるが、**知覚閾値を
  超える成分はわずか 13 個**（全体の 0.00009%）で、これは 3,790,160 画素中せいぜい
  数画素の一部チャンネルにしか現れない。これは「画素差 0」を達成した `px`/`py`/`rz`
  ほど厳密ではないが、実測で知覚下（sub-pixel の AA ノイズ）であることを数値で示せた
  ため、**6 桁のまま採用する**（当て推量ではなく、この実測が根拠。もし知覚閾値超えが
  数千〜数万個あれば桁を上げる判断だったが、実際は 13 個だった）
  - 対照実験として 3 桁／9 桁でも同じ手法（`setCameraView` の直接呼び出し）で比較した：
    3 桁は誤差 4.72e-4 で差分成分 830,615（5.48%）・最大成分差 183/255、9 桁は誤差
    4.89e-10 でビット同一（差分 0）。6 桁は両者の間で、9 桁側（無視できるレベル）に
    近いことが「知覚閾値超え 13 個」という数値からも裏付けられる
  - **原因はカメラ視点が画面全域のあらゆる線のアンチエイリアス縁に影響するため**
    （1 個の剛体の並進と違い、投影角そのものが動く）。`px`/`py`/`rz` が画素差 0 を
    達成できたのは対象が 1 個の剛体だけを動かすのに対し、カメラは全ビーム・全エッジの
    投影を同時に動かすため、AA 縁の総面積が桁違いに大きいことが差分成分数の違いを説明する
- **★実ドラッグ→共有 URL→フルリロードの end-to-end 往復で、視点が視覚的に復元することを
  CDP で確認した。** Escape でギズモを外し、左ドラッグ（回転）・右ドラッグ（パン）の
  後、`cameraChangeRunCount` が動かなくなるまでポーリングして慣性の収束を待った。
  - ビーム指紋は往復の前後で不動（`-856341260`）——光路計算に一切触れないことを再確認
  - position/target の往復誤差は 1.1e-5 〜 1.2e-4（6 桁丸め単体の理論誤差 5e-7 より
    2 桁大きい）。差分成分は 918,430／15,160,640（6.06%）、知覚閾値超えは 2,158 成分
    （画素換算で概算 540 画素／3,790,160 画素 ≈ 0.014%）、最大成分差 183/255
  - **この上振れは URL エンコードの精度ではなく、下の★C で確認した慣性の残差が原因。**
    実ドラッグでは「URL に書き込まれた瞬間の視点」と「その後さらに測定した視点」の間に
    ズレが生じうるため、精密対照実験（上表）より誤差が 1〜2 桁大きく出る。それでも
    知覚閾値超えは全体の 0.014% にとどまり、通常の閲覧で気づく差ではない
- **★C: 最終書き込みが「慣性減衰後の確定視点」かどうかを、追加 3 秒の待機を挟んで
  2 回測定し直して確認した。**
  - `cameraChangeRunCount`（`'change'` 発火回数）は 1 回目の静止確認から追加 3 秒後も
    `134` のまま変化なし——`'change'` は本当に止まっている
  - `urlWriteCount`（`replaceState` 回数）も追加 3 秒後まで `3` のまま変化なし——
    これ以上の書き込みは発生しない。**書き込みは確かにこの時点で確定している**
  - URL 文字列（`location.hash`）も追加 3 秒後まで完全に同一——書き込まれた `cam` 値は
    その後変わらない
  - **ただし、生のカメラ位置は追加 3 秒の間にごくわずかに動いていた**
    （position.x が `-4.458481137142902` → `-4.458467175351911`、差にして 1.4e-5）。
    `'change'` イベントは `OrbitControls` 内部の EPS 閾値（1 フレームの移動量が
    約 0.001 未満）を超えたときだけ発火するため、**閾値未満の極小な慣性の尾が
    `'change'` に検出されないまま残ることがある**、という事実がここで確認できた。
    これが上の「実ドラッグの往復誤差が精密対照実験より 1〜2 桁大きい」ことの直接の原因。
  - **結論：** 最終書き込みは中間視点で終わっているわけではなく（`'change'`/
    `urlWriteCount`/URL 文字列はすべて安定後の値で確定している）、`OrbitControls` の
    設計上の限界（EPS 未満の残差検出漏れ）によって「確定」の基準そのものが
    数万分の一単位で緩い、というのが実態である。デバウンス機構自体に見直すべき欠陥は無い
- **CDP 実測のまとめ（測定 URL・状態を併記）**：
  - 測定 URL: `#v=1&a=70&mat=sf10&sa=0.324229,0.351336,-11.371&sec=1`
  - 既定視点のまま読み込むと `cam` キーは出ない（実測ハッシュに `cam=` を含まない）
  - 実マウスドラッグ（Escape でギズモを外してカメラ専用操作にし、左ドラッグ=回転・
    右ドラッグ=パン）で position・target とも変化することを確認
  - 慣性収束後の URL には `cam=` が正しい 6 値カンマ区切りで載る
    （実測例: `cam=-4.45856,-4.176981,0.784723,0.151103,0.405623,0.757504`）
  - リロードでの復元後もビーム指紋は不動（`-856341260` のまま）——カメラの往復は
    光路計算に一切触れないことの追加確認
  - WYSIWYG: 既定視点の PNG と視点移動後の PNG は明確に異なる
    （実測: 長さ 2,583,346 文字 → 1,831,234 文字、内容が異なる）
  - hashchange（同一タブでの `location.hash` 直書き、リロード無し）で `cam=1,2,3,4,5,6`
    が反映されることを確認（浮動小数点で最終桁に ~1e-16 のノイズが乗るが、これは
    `setCameraView` が `orbit.update()` で球面座標を経由して位置を再構成するために
    生じる機械精度ノイズであり、6 桁 URL の精度予算 1e-6 よりさらに 10 桁小さいため
    無視できる）
  - console エラー 0（dev server・本番 `vite preview` 双方で確認）
  - 本番ビルド: `cam=` を含む URL を `vite preview` で開いても例外なし・canvas 描画あり。
    `__debug`（`cameraChangeRunCount` 等）はビルドで完全に消え、`grep` で 0 件を確認
    （`setCameraView`/`cameraTarget` は `InteractionCtl` の本体メソッドとして正しく残る）

### Phase 6 完了条件
- ✅ 断面 2D ビューが 3D ビューと同じ光路を示す
- ✅ 「最小偏角を探索」実行後の角度が最小偏角の式を満たす
- ✅ 仮想スクリーンに赤〜紫のスペクトル帯が映る
- ✅ フレネル反射が入射面に薄く現れ、入射角を上げるほど明るくなる
- ✅ 拡張機能を全て有効にしても 60 fps を維持している

---

## 依存関係図

```
Phase 0（環境構築）
 └─ Phase 1（光学コア・テスト駆動）★ここを固めてから描画に進む
     └─ Phase 2（3D 描画）
         └─ Phase 3（インタラクション）
             └─ Phase 4（UI・数値表示）
                 └─ Phase 5（仕上げ・検証）
                     └─ Phase 6（拡張機能）
                         ├─ 6-1 断面 2D ビュー
                         ├─ 6-2 最小偏角の自動探索
                         ├─ 6-3 仮想スクリーン ─→ 6-5 フレネル反射描画
                         └─ 6-4 フレネル反射率 ─┘
```

---

## 進捗管理メモ

- **開始日**: 2026-08-10
- **開発体制**: 1 名（AI + ユーザー）
- **確認タイミング**: 各フェーズの完了条件を満たした時点
- **テスト方針**: `src/optics/` は実装前にテストを書く。DOM の構造・状態束縛は jsdom で Vitest、
  実描画・画素・実レイアウト・end-to-end は CDP（CLAUDE.md「テスト方針」節）
- **既知 debt（4-3 で確認）**: `__debug` の表面は `import.meta.env.DEV` ブロックごと本番バンドルから
  消えるが、`memoryInfo` / `rayLineCount` / `pathCount` / `quadCount` のような public な検証用
  ゲッターはクラス本体の一部としてバンドルに残る。到達経路が無いので**機能的な漏れではない**
  （サイズのみの debt）
- **解消済み debt（2026-08-28 実機スモークで確認）**: PNG 保存ボタンの実ブラウザでの最終着地
  （ダウンロード完了）を確認した。CDP（ヘッドレス）では `canceled` で止まり、4通り試して
  同じ地点だったためハーネス制約と判断していたが、実ブラウザ（Chrome、`npm run build` →
  `npm run preview`）でボタンを押すとファイルが実際に降り、画面の表示と一致することを確認した
- **解消済み debt（監査で確認、2026-08-30 の実測で 5-4 クローズ）**: 5-4「メモリリークの確認
  （dispose が効いているか）」は前提がズレていた。材質切替は `PrismObject` を dispose+再生成
  せず `main.ts:792`（当時 `main.ts:753`）`prism.setRefractiveIndex(material.catalogNd)` で
  インプレース書き換えする設計であり、5-4 着手時は「dispose の効き」ではなく「インプレース
  書き換え（および他の資源確保箇所）で GPU 資源が増え続けないこと」の確認へ読み替えた。
  読み替え後の検証結果は 5-4 の設計メモを参照
- **実機スモークテスト（2026-08-28）**: `npm run build` → `npm run preview`（dist の実配信）を
  実ブラウザ（Chrome）で実行。シナリオ A/B/C（5-3 の WebGL2 非対応判定を dev サーバではなく
  この本番ビルド上で再実行したもの）すべて PASS、console エラー 0 を確認。既定状態
  （入射角 53.3° / BK7 / 断面図 OFF）と、操作を加えた状態の両方で確認済み。この結果を根拠に、
  PNG 保存ボタンの実着地確認・G4（Enter発火）・5-5（ビルド成果物の動作確認）をクローズした。
  この 3 件は同一スモークセッションの証拠を共有するが、PNG 着地・G4 は 5-5 の下位項目ではなく
  それぞれ独立した debt/監査項目であり、5-5 自体の根拠は「シナリオ A/B/C」と「console エラー0」
  の 2 点（コンパイル・minify 後のバンドルでも WebGL2 判定分岐と描画が壊れていないことの確認）
  である
- **5-5 を単一ブラウザのスモークで [x] 化した根拠（コミット `08a6cb5` 本文より転記）**：
  「SPEC.md の対応ブラウザ要件が Chrome 単独でクロスブラウザを求めないため、今回のスモークで
  基準を満たすと判断し `[x]` 化」。SPEC.md の該当行（対応ブラウザ: `Chrome 最新版（WebGL2
  必須）。非対応時は明示的なメッセージを表示`）はブラウザ横断の要求を含まないため、複数ブラウザ
  での確認は 5-5 のスコープ外
- **`[x]` の信頼根拠**: 2026-08-28 の監査時点で `npm run test` が 872 passed (872) / Test Files
  30 passed (30)、`npm run build` 成功、作業ツリークリーンを確認。個別行の証拠（テスト・CDP・
  コミット）はハッシュを併記した行、または対応コミットの diff を参照する
- **リスク**: Phase 6-5（フレネル反射）は光線本数の増加でパフォーマンス要件に抵触する可能性がある。6-3 の時点で負荷を測っておく
