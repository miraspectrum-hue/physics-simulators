# SPEC.md - 機能仕様・技術スタック・UI/UX要件

## 目的・ターゲットユーザー

**目的**
プリズムによる光の分散（スペクトル分離）を「触って確かめられる」形で提示し、入射角・屈折率・波長の関係を直感的に理解できるようにする。参考イメージ `prizm.png` に近い見た目を保ちながら、物理的に正しい光路計算に基づいて描画する。

**ターゲットユーザー**
- 光学・物理を学ぶ高校生〜大学生、およびその指導者（授業でのデモ利用）
- 分光の挙動を手早く確認したい技術者・ホビイスト

**使用シーン**
PC ブラウザでの自習、授業でのライブデモ、プレゼンテーション

---

## 機能一覧

### 必須機能

| # | 機能 | 説明 | 優先度 |
|---|------|------|--------|
| F-01 | 正三角柱プリズムの 3D 表示 | 頂角 60° の正三角形断面を押し出した透明体。ガラス質感で描画 | 🔴 高 |
| F-02 | 白色光の入射 | 単一の白色ビームがプリズム側面に入射。入射点と方向を可視化 | 🔴 高 |
| F-03 | 波長依存の屈折計算 | Cauchy の分散式で λ ごとに n を求め、スネル則で屈折させる | 🔴 高 |
| F-04 | スペクトル分離の描画 | 射出光を波長ごとの色付き光線束として描画（赤が最小偏角側） | 🔴 高 |
| F-05 | 入射角の変更 | スライダーで入射角 θ₁ を -89°〜89° で連続変更。光源は**主断面内**でのみ振れる（1 自由度） | 🔴 高 |
| F-06 | プリズムの回転 | ギズモ（TransformControls）と X/Y/Z 軸スライダーの併用。光路が即座に追従 | 🔴 高 |
| F-07 | プリズムの移動 | ギズモと位置スライダーで平行移動。ビームから外れれば光は直進する | 🔴 高 |
| F-08 | カメラ操作 | 軌道回転・パン・ズーム（マウス操作） | 🔴 高 |
| F-09 | 全反射の処理と描画 | 臨界角を超えた面では反射させ、内部反射の光路も描画する | 🔴 高 |
| F-10 | リセット | 初期姿勢・初期入射角へ復帰 | 🔴 高 |

### あれば嬉しい機能

| # | 機能 | 説明 | 優先度 | 実装 |
|---|------|------|--------|------|
| F-21 | 数値インフォパネル | 入射角・屈折角・偏角 δ(赤/紫)・n(λ)・全反射の有無を表示 | 🟡 中 | Phase 4 |
| F-22 | 材質の切替 | BK7 / SF10 / 水 / ダイヤモンド（全反射デモ）を選択。分散量が変わる | 🟡 中 | Phase 4 |
| F-23 | 分散の誇張倍率 | 教育用に `(n(λ) - n_d)` を ×1〜×10 して分離を強調 | 🟡 中 | Phase 4 |
| F-24 | スペクトル表示モード | 連続スペクトル / 可視 7 色（赤橙黄緑青藍紫）離散 の切替 | 🟡 中 | Phase 4 |
| F-25 | Bloom によるグロー表現 | `prizm.png` のような発光感を付与（ON/OFF 可） | 🟡 中 | Phase 2 |
| F-26 | 断面 2D ビュー | 主断面を真横から見た図を小窓に併記（角度の理解を助ける） | 🟡 中 | Phase 6 |
| F-27 | 最小偏角の自動探索 | ボタン一つで最小偏角となる入射角へアニメーション遷移 | 🟡 中 | Phase 6 |
| F-28 | フレネル反射の描画 | 各界面での部分反射を反射率に応じた明るさで薄く描画 | 🟡 中 | Phase 6 |
| F-29 | 仮想スクリーン投影 | 射出側にスクリーンを置き、虹のスペクトル帯を映す | 🟡 中 | Phase 6 |
| F-30 | 設定の URL 共有 / PNG 書き出し | 現在の状態を復元可能なリンク・画像として保存 | 🟢 低 | 任意 |

---

## 光学モデル（計算仕様）

本節の式は実装と一字一句対応させること。

### 屈折率

Cauchy の分散式を採用する。

```
n(λ) = A + B / λ²        （λ の単位は µm）
```

**基準スペクトル線**（本仕様で用いる波長。すべてここに統一する）

| 記号 | 名称 | 波長 | 用途 |
|------|------|------|------|
| λ_d | ヘリウム **d 線** | **587.56 nm** | n_d とアッベ数 v_d の定義波長。分散誇張・最小偏角探索の基準 |
| λ_F | 水素 F 線 | 486.13 nm | アッベ数の算出 |
| λ_C | 水素 C 線 | 656.27 nm | アッベ数の算出 |

> **注意**: 589.3 nm はナトリウムの **D 線**であり、d 線とは別物である。n_d / v_d の定義は d 線（587.56 nm）を用いるため、本仕様では 589.3 nm を使わない。両者の差は BK7 で Δn = 7.2e-5、Δδ_min = 0.006° と小さいが、検証許容値（δ_min ±0.05°）の 13% を無駄に消費するため統一する。

| 材質 | A | B [µm²] | n_d (587.56nm) | アッベ数 v_d | 臨界角 θc | 60°プリズムでの透過 |
|------|---|---------|----------------|--------------|-----------|-------------------|
| BK7（ホウケイ酸クラウン） | 1.5046 | 0.004200 | 1.5168 | 64.4 | 41.2° | ○（既定材質） |
| SF10（重フリント） | 1.6894 | 0.013420 | 1.7283 | 28.4 | 35.4° | ○（分散が最大） |
| 水（20°C） | 1.3243 | 0.003134 | 1.3334 | 55.7 | 48.6° | ○（分散が最小） |
| ダイヤモンド | 2.3784 | 0.013420 | 2.4173 | 55.3 | 24.4° | **×（常に全反射）** |

> **⚠ ダイヤモンドは頂角 60° のプリズムを透過できない**
> プリズムを光が透過するには `頂角 A < 2·θc` が必要である（第一面での屈折角の上限が θc であり、`r₁ + r₂ = A` かつ `r₂ < θc` を満たす必要があるため）。ダイヤモンドは `60° > 2 × 24.4° = 48.9°` となり、**入射角をどう変えても第二面で必ず全反射する**。
>
> これは仕様上のバグではなく物理的事実であり、ダイヤモンドが強く輝く理由そのものである。したがってダイヤモンドは削除せず、**全反射のデモンストレーション用**として材質リストに残す。ただし以下の対応を必須とする。
> - UI の材質セレクトで「ダイヤモンド（全反射デモ）」と明示する
> - 選択時は情報バーに「頂角 60° では射出しません（常に全反射）」と警告を表示する
> - F-27（最小偏角の自動探索）はダイヤモンド選択時に無効化する（解が存在しないため）
> - 透過条件 `A < 2·θc` を判定する関数を `optics/prism.ts` に用意し、テストで全材質を検証する

> **係数の決定方法（重要）**
> 一般に流通している Cauchy 係数表（例: SF10 に A=1.7280, B=0.01342 を与えるもの）は、d 線での屈折率がカタログ値と最大 0.04 ずれる。本アプリでは以下の手順で係数を確定した。
> 1. B は各材質の**アッベ数 v_d が実測値と一致する**ように決める
>    `B = ((n_d - 1) / v_d) / (1/λ_F² - 1/λ_C²)`（λ_F = 0.48613µm, λ_C = 0.65627µm）
> 2. A は **n(λ_d) がカタログの n_d と一致する**ように逆算する
>    `A = n_d - B / λ_d²`（λ_d = 0.58756µm）
>
> この 2 条件により、屈折の絶対量（A 由来）と分散の大きさ（B 由来）の両方が実測と整合する。係数を変更する際は必ずこの検算を通すこと。
>
> **裏取り済み**: 上記 Cauchy 係数は、メーカー公表の **Sellmeier 式による独立計算**と突き合わせ済み。
> 出典 — N-BK7 / SF10: SCHOTT データシート、水: Daimon & Masumura, *Appl. Opt.* 46 (2007) 20.0°C、
> ダイヤモンド: Phillip & Taft (1964)。n_d は全材質で差 ≤ 0.0004、アッベ数は差 ≤ 0.12 で一致した。

**責務分離**: `refractiveIndex()` は可視域（`WAVELENGTH_MIN/MAX_NM`）の検証をしない純粋な数学関数とし、可視域チェックは上位層（`spectrum.ts` や呼び出し側）の責務とする。不正値（正の有限数でない波長）に対しては `RangeError` を投げる。

**分散誇張（F-23）**: 倍率 m に対し `n'(λ) = n_d + m × (n(λ) - n_d)` を用いる。m=1 で物理的に正しい値となる。

### 波長サンプリングと色

- 可視域: 380〜750 nm
- **連続モード**: 48 サンプル（等間隔）
- **7 色モード**: 660 / 610 / 580 / 550 / 480 / 450 / 410 nm を代表値とする（赤・橙・黄・緑・青・藍・紫）
- 波長 → sRGB は Bruton の近似式を用い、可視域の両端では視感度に応じて強度を減衰させる

### プリズムの標準配置

`optics/convexSolid.ts` が生成する平面集合の基準姿勢（ワールド変換を適用する前の形）を以下に固定する。

| 項目 | 規約 | 理由 |
|------|------|------|
| 断面の平面 | **XY 平面** | 主断面が XY となり、F-05「光源は主断面内でのみ振れる」が XY 内の 1 自由度になる |
| 押し出し軸 | **Z 軸** | 既定カメラが −Z を見るため初期状態で断面が正対する。F-26 の断面 2D ビューは Z 軸に沿った正射投影で足りる |
| 頂角の向き | **+Y（上）** | 教科書の図・`prizm.png` と同じ。底辺が下、左右の面が入射面／出射面になる |
| 原点 | **重心** | 回転が重心まわりになる。正三角形では重心＝内心なので側面 3 枚の距離が内接円半径 r で揃う |
| 平面の順序 | `[左側面, 右側面, 底面, 前端面(+z), 後端面(-z)]` | テストと描画層が同じ並びを前提にできる |

一辺 `a`・押し出し長 `L` は引数で受け取る（**頂角は 60° 固定**。F-01 が正三角形と定めるため）。`r = a/(2√3)`、外接円半径 `R = a/√3`。側面 3 枚の法線は底面法線 `(0,-1,0)` を ±120° 回転したもので、距離はいずれも `r`。端面は `(0,0,±1)`、距離 `L/2`。

### 光路計算

1. プリズムを **凸多面体（外向き法線つき平面 5 枚：側面 3 + 端面 2）** として表現する
   - **追跡はプリズムの局所空間で行う。** 平面は「プリズムの標準配置」のまま固定し、変換しない
   - ワールド⇔局所の変換は **scene 層の責務**とする。入射レイを `Object3D.matrixWorld` の逆変換で局所空間へ引き込み、得られた光路を順変換でワールドへ戻す
   - 「ジオメトリを変換せず、レイを変換する」レイトレーシングの定石に従う。平面 5 枚を毎フレーム変換する代わりにレイ 1 本を変換するだけで済み、法線の逆転置も不要になる
   - **剛体変換（回転・平行移動）に限定する。スケールは扱わない**（回転行列は直交なので法線が同じ回転で移り、レイの方向ベクトルも単位長を保つ）
   - `optics/` は変換を一切受け取らない。姿勢の単一の真実は `Object3D.matrix` のまま
2. レイと各平面の交差から入口 `t_enter` / 出口 `t_exit` を求める（スラブ法）
   - `t_enter > t_exit` なら交差なし＝光は直進する
3. 交点の面法線に対し、**ベクトル形式のスネル則**を適用する

```
η = n₁ / n₂
cosθᵢ = -d · n
k = 1 - η²(1 - cosθᵢ²)

k ≥ 0 → 屈折:   t = ηd + (η·cosθᵢ - √k)·n
k < 0 → 全反射: r = d - 2(d · n)n
```

4. 屈折・反射を**最大 6 回**まで追跡する。射出後は一定長（既定 40 単位）だけ延長して打ち切る
5. 上記を波長サンプルごとに独立に実行し、折れ線の配列 `LightPath[]` を得る

### フレネル反射（F-28 / Phase 6）

無偏光として s 偏光・p 偏光の平均を用いる。

```
R = (Rs + Rp) / 2
Rs = |(n₁cosθᵢ - n₂cosθₜ) / (n₁cosθᵢ + n₂cosθₜ)|²
Rp = |(n₁cosθₜ - n₂cosθᵢ) / (n₁cosθₜ + n₂cosθᵢ)|²
```

反射光は分岐により光線本数を倍増させるため、**追跡本数の上限管理を実装前に決めること**（既定: 反射光は 1 回のみ分岐、以降は追跡しない）。

### 検証に使う既知値（テスト用）

| 検証項目 | 期待値 |
|----------|--------|
| 最小偏角の関係式 | 頂角 A=60° に対し `n = sin((A + δ_min)/2) / sin(A/2)` が材質の n_d と一致 |
| 臨界角 | `θc = asin(1/n)` — BK7 で約 41.2°、ダイヤモンドで約 24.4° |
| 垂直入射 | 入射角 0°（面に垂直）のとき第一面で偏向が起きない |
| n_d 一致 | 各材質の n(587.56nm) がカタログ値と ±0.001 で一致 |
| アッベ数 | `(n_d - 1)/(n_F - n_C)` が実測値と ±1.0 で一致 |
| 赤紫の順序 | 常に δ(赤) < δ(紫)（赤の方が偏角が小さい） |
| 透過可否 | `A < 2·θc` の判定が BK7/SF10/水 で true、ダイヤモンドで false |
| ダイヤの全反射 | ダイヤモンドでは入射角 -89°〜89° の全域で射出光が生じない |

**参考値（頂角 60°、d 線 587.56nm での最小偏角）**

`δ_min = 2·asin(n_d · sin(A/2)) - A` により算出。Sellmeier 式による独立計算で裏取り済み（差 ≤ 0.006°）。

| 材質 | δ_min | Sellmeier による検算値 |
|------|-------|----------------------|
| BK7 | 38.65° | 38.647° |
| SF10 | 59.57° | 59.569° |
| 水 | 23.63° | 23.626° |
| ダイヤモンド | 解なし（全反射） | 解なし（`n_d·sin30° > 1`） |

---

## 画面・コンポーネント設計

### 画面構成（単一画面）

```
┌──────────────────────────────────────────────────────────────┐
│  プリズム分光シミュレータ                    [?ヘルプ] [リセット] │ ← ヘッダー
├───────────────────────────────────────────┬──────────────────┤
│                                           │ 操作パネル       │
│                                           │                  │
│        ╱╲                                 │ ■ 光源           │
│   ─── ╱  ╲ ══════▶ 🌈                    │  入射角  [──●──] │
│      ╱____╲                               │  ビーム幅[─●───] │
│                                           │                  │
│      （3D ビューポート）                   │ ■ プリズム       │
│                                           │  材質  [SF10  ▾] │
│  ┌──────────┐                             │  操作  ○カメラ   │
│  │ 断面2Dビュー│                            │        ●移動    │
│  └──────────┘                             │        ○回転    │
│                                           │  回転 X [──●──] │
│                                           │       Y [──●──] │
│                                           │       Z [──●──] │
│                                           │  [最小偏角を探索] │
│                                           │                  │
│                                           │ ■ 表示           │
│                                           │  ○連続  ●7色    │
│                                           │  分散倍率[●────] │
│                                           │  ☑ グロー        │
│                                           │  ☑ 数値表示      │
│                                           │  ☑ スクリーン    │
│                                           │  ☐ フレネル反射  │
├───────────────────────────────────────────┴──────────────────┤
│ θ₁=45.0° │ n(656)=1.7208 n(405)=1.7714 │ δ赤=48.2° δ紫=53.9° │ ← 情報バー
└──────────────────────────────────────────────────────────────┘
```

### モジュール構成

| モジュール | 責務 | 依存 |
|-----------|------|------|
| `main.ts` | 初期化・全体配線 | 全て |
| `ui/store.ts` | アプリ状態の単一保持と変更通知 | - |
| `optics/constants.ts` | 波長範囲・材質定数・頂角・追跡上限 | - |
| `optics/dispersion.ts` | λ → n の算出 | constants |
| `optics/spectrum.ts` | 波長サンプリング、λ → sRGB | constants |
| `optics/vec3.ts` | 3 次元ベクトルの純粋演算（Three.js 非依存） | - |
| `optics/convexSolid.ts` | 平面集合の生成、レイ交差 | vec3 |
| `optics/fresnel.ts` | 臨界角・全反射判定（`criticalAngle` / `canTransmit`）と反射率 R の算出 | - |
| `optics/refraction.ts` | スネル則による屈折角（スカラー・幾何非依存の純粋関数） | fresnel |
| `optics/prism.ts` | 頂角 A のプリズムのスカラー計算（偏角・最小偏角・将来の透過条件） | refraction |
| `optics/tracer.ts` | 屈折・全反射・光路生成 | dispersion, convexSolid, fresnel, refraction |
| `scene/SceneManager.ts` | レンダラ・カメラ・ポストエフェクト・ループ | three |
| `scene/PrismObject.ts` | プリズムメッシュと姿勢、ワールド⇔局所のレイ／光路変換 | three, convexSolid |
| `scene/BeamRenderer.ts` | `LightPath[]` → Line2 群の生成・更新 | three, spectrum |
| `scene/ScreenObject.ts` | 仮想スクリーンとスペクトル帯の描画 | three, spectrum |
| `scene/SectionView.ts` | 断面 2D ビュー（正射投影の副シーン） | three |
| `scene/InteractionCtl.ts` | Orbit / Transform コントロールの排他切替 | three |
| `ui/ControlPanel.ts` | 入力 UI と store の双方向束ね | store |
| `ui/InfoOverlay.ts` | 計算結果の数値表示 | store |

### デザイン

- **テーマ**: `prizm.png` に合わせたダークテーマ固定
  - 背景: `#05070d` → `#0d1220` の緩やかなグラデーション
  - プリズム: 青味がかった透明ガラス（`color: #4a6ea8`, `transmission: 0.95`, `ior` は選択材質に連動）
  - パネル: `#111826` 半透明 + backdrop-blur、文字 `#e6ecf5`、アクセント `#5b8dff`
  - 危険/警告（全反射発生の表示など）: `#ffb454`
- **光線**: 加算ブレンドで描画し、床面に淡い反射を落とす
- **フォント**: システムフォント（日本語は Noto Sans JP / Yu Gothic フォールバック）
- **レイアウト**: 操作パネル幅 320px 固定、ビューポートは残り全域。断面 2D ビューは左下に 240×180px

---

## 技術スタック

```
├─ TypeScript 5.9.3 (strict)
├─ Vite 8.2.1 (ビルド・開発サーバ)
├─ Three.js 0.185.1 (WebGL2)
│   ├─ @types/three 0.185.4    （three は型を同梱しないため必須）
│   ├─ OrbitControls / TransformControls
│   ├─ Line2 / LineMaterial      （太さのある光線描画）
│   ├─ EffectComposer / UnrealBloomPass （グロー）
│   └─ MeshPhysicalMaterial      （transmission / ior でガラス表現）
└─ Vitest 4.1.10 (src/optics/ の単体テスト)
```

### バージョン固定（確定）

`package.json` にはキャレット（`^`）を付けず、以下の完全な番号で固定する。

| パッケージ | 固定版 | 区分 | 根拠 |
|-----------|--------|------|------|
| `three` | `0.185.1` | dependencies | npm `latest` タグの実在最新版 |
| `@types/three` | `0.185.4` | devDependencies | three 0.185 系に対応する型定義 |
| `typescript` | `5.9.3` | devDependencies | 5.x 系の最終版。TS 7.0.2 が latest だが新コンパイラの初期リスクを避ける |
| `vite` | `8.2.1` | devDependencies | npm `latest`。`engines: node ^20.19.0 \|\| >=22.12.0` |
| `vitest` | `4.1.10` | devDependencies | npm `latest`。`peerDependencies.vite: ^6 \|\| ^7 \|\| ^8` と整合 |

- Node.js は **20.19 以上または 22.12 以上**が必要（Vite 8 の engines 要件）
- 依存は npm で固定する。CDN からの実行時読み込みは行わない
- **未検証**: 上記はレジストリ上の peer 範囲と engines を突き合わせた結果であり、実際に install してビルドが通ることは未確認。Phase 0-2 で最初に検証すること

---

## 非機能要件

| 項目 | 要件 |
|------|------|
| **パフォーマンス** | 1920×1080 / 連続 48 波長で 60 fps を維持。光路の再計算は 2 ms 以内 |
| **応答性** | スライダー操作から描画反映まで 1 フレーム以内（16 ms） |
| **対応ブラウザ** | Chrome 最新版（WebGL2 必須）。非対応時は明示的なメッセージを表示 |
| **対応デバイス** | デスクトップのみ。タッチ最適化は対象外 |
| **セキュリティ** | 通信・永続化・外部送信を一切行わない完全ローカル動作。ユーザーデータを扱わない |
| **起動** | `npm run dev` で開発。`npm run build` で静的ファイルを出力（サーバ不要） |
| **アクセシビリティ** | 全操作をキーボードで到達可能。数値は必ずテキストでも表示し、色のみに依存しない |
| **保守性** | 光学ロジックを描画から完全分離し、単体テストで検証可能に保つ |
| **数値的健全性** | 光路計算の結果に NaN / Infinity を含めない。全反射・かすめ入射でも破綻しない |

---

## 今後の拡張案（未着手）

- 複数プリズムの連結配置（分散の打ち消し・アクロマート構成の実演）
- 回折格子との比較モード
- 偏光の可視化（s / p 成分の分離表示）
- 設定の URL 共有 / PNG 書き出し（F-30）
